# Phase 6-D2B-R1 implementation closure — 2026-09-23

Status: **IMPLEMENTED / CI_PENDING**

Canonical goal: `docs/goals/phase-6-d2b-r1-dispatch-hardening.md`

Selection audit: `docs/status/phase-6-d2b-r1-review-2026-09-22.md`

Coding-agent handoff: `docs/status/phase-6-d2b-r1-next-coding-agent-goal.md`

## Baseline

The implementation began from a clean, fast-forward-only `main`:

```text
local HEAD: 3545794aa1a192374e5ce90c8fd806bc7b851923
origin/main: 3545794aa1a192374e5ce90c8fd806bc7b851923
working tree: clean
```

The baseline's own CI state was verified before any edit: GitHub Actions
run `35695543233` (workflow `CI`) has `head_sha` exactly
`3545794aa1a192374e5ce90c8fd806bc7b851923` and conclusion `success`.

## Delivered boundary

Phase 6-D2B-R1 hardens the existing D2B delivery boundary in place. D1,
D2A/R1, Phase 6-A/6-B/6-C, `strict-v1`, valuation, hard-gate, monitoring
cursor and re-analysis semantics are untouched.

- **Durable dispatch-start evidence (F1).** The new immutable, canonical,
  hash-validated `MonitoringDispatchClaimV1` contract (checked-in schema
  `schemas/monitoring-delivery-dispatch-claim.schema.json`) is persisted
  through the existing immutable create-only store path **before** the
  transport may write any request byte, but after in-memory endpoint/auth
  resolution (an unresolved endpoint can therefore never manufacture a
  claim). Claim content is fully deterministic — no wall-clock field — so
  re-saving the same claim for a resumed attempt is byte-identical and can
  never raise a spurious immutable-conflict. A claim without its terminal
  attempt artifact is an *orphaned dispatch*: `derive_delivery_state`
  derives terminal `AMBIGUOUS` with the new `ORPHANED_DISPATCH` error code
  and zero automatic resend for the default
  `receiver_idempotency_declared=false`; only the explicit
  `receiver_idempotency_declared=true` policy permits a bounded retry of the
  same deterministic delivery key (`attempt_number == attempts + 1`,
  monotonic), and never beyond `max_attempts`. Multiple unresolved claims, a
  non-monotonic unresolved number, or a claim bound to a different
  intent/payload hash fail closed.
- **Per-delivery OS single-flight exclusion (F2).** The new
  `monitoring_delivery/locking.py` takes a non-blocking exclusive `flock` on
  `delivery_root/locks/<delivery_id>.lock` as the first and only liveness
  authority, held across intent persistence, state validation/repair,
  dispatch-claim publication, the transport call, attempt persistence and
  latest-state publication. Only the platform's real contention errors
  (`BlockingIOError`/`EAGAIN`/`EWOULDBLOCK`/`EACCES`) map to
  `DeliveryLockBusyError`/CLI `DELIVERY_BUSY` (new additive exit 3, zero
  outbound requests); every other open/flock/filesystem error fails closed
  as `DeliveryLockError` (exit 2) and is never disguised as busy. The lock
  file carries no mutable holder record, and the receiver's
  `Idempotency-Key` is never relied on to resolve local concurrency.
- **One monotonic end-to-end HTTP deadline (F3).** `WebhookHttpTransport`
  now treats `timeout_seconds` as one overall monotonic deadline from an
  injectable clock. The connect/TLS phase receives the full remaining
  budget; the request-write/response-header wait and **every** bounded
  response-body read each re-derive and re-apply only the remaining budget
  through `sock.settimeout`. Deadline expiry proven to precede any request
  byte is honestly retryable (`DEADLINE_EXHAUSTED_PRE_DISPATCH`, new code);
  expiry after dispatch may have started — including between repeated body
  reads — is `AMBIGUOUS`/`TIMEOUT_AFTER_DISPATCH`. A `TimeoutError` or
  connection loss during body reads is now also classified
  post-dispatch `AMBIGUOUS` instead of escaping as an unclassified error.
- **Truthful read-only pointer status (F4).**
  `delivery_status_projection`/`tve watch delivery-status` now *validate*
  the published latest-state pointer instead of reporting path existence:
  `pointer_status` is `MISSING` (nothing published, not repaired by
  status), `CURRENT` (canonical and byte-identical to the immutable-artifact
  derivation) or `STALE_REPAIRABLE` (valid but behind the immutable
  artifacts; only the mutating delivery path may repair it, and the
  repair-behind rule is shared with the service through
  `published_state_is_behind`). Corrupt, non-canonical, foreign-slot and
  contradictory pointers fail closed with `DELIVERY_POINTER_CONFLICT`
  (CLI exit 2). The projection also reports dispatch-claim evidence
  (`dispatch_claim_count`, `unresolved_dispatch_claim`) for each delivery.

No endpoint, token or other secret value enters any dispatch claim,
attempt, state, status projection, lock slot, CLI output or exception text;
the existing secret-canary matrix continues to scan every ledger file
including the new claim and lock paths.

## Automatic verification

`tests/test_monitoring_delivery.py` grows from 43 to **62 deterministic
cases** (19 new, one crash-window test rewritten to inject its crash before
the new claim boundary, which is the R1-correct retryable point). No public
Internet, real sleep-based race, live provider, model or owner input is used
anywhere.

Findings → automatic evidence:

- **F1 crash windows**:
  `test_crash_after_claim_before_send_is_conservative_ambiguous_zero_resend`
  (claim durable, send never happened → restart `AMBIGUOUS`, `ORPHANED_DISPATCH`,
  zero resend on the first and every later wake),
  `test_crash_after_possible_dispatch_before_outcome_save_is_ambiguous`
  (request bytes may have been written → restart `AMBIGUOUS`, exactly one
  outbound request ever),
  `test_orphaned_claim_retries_only_under_declared_receiver_idempotency`
  (opt-in policy retries the orphan with the identical deterministic key,
  byte-identical claim re-save, monotonic attempt numbering 1→2),
  `test_orphaned_claim_with_exhausted_budget_stays_terminal_ambiguous`
  (an orphan can never buy extra retry budget),
  `test_unresolved_claim_conflicts_fail_closed` (multiple orphans and
  foreign-bound claims fail closed),
  `test_crash_after_intent_before_claim_resumes_same_delivery` (a crash
  before any claim remains safely retryable with zero D1/provider reruns).
- **F2 single-flight**:
  `test_two_processes_single_flight_only_one_enters_transport` — a real
  `subprocess` child enters the transport and blocks on a deterministic
  file barrier while holding the OS lock; the parent process simultaneously
  fails fast with `DeliveryLockBusyError` and zero transport calls; after
  release the winner's result is `DELIVERED` with exactly one HTTP request
  and a later wake replays with zero,
  `test_real_contention_is_classified_busy_not_fail_closed`,
  `test_non_contention_lock_failure_fails_closed_not_busy` (injected
  `ENOLCK` flock and `EIO` open failures are `DeliveryLockError`, never
  busy, zero requests),
  `test_cli_deliver_busy_exits_three_with_zero_requests` (additive CLI
  exit 3 `DELIVERY_BUSY` while a real flock is held).
- **F3 overall deadline**:
  `test_overall_deadline_bounds_connect_request_and_reads_from_one_budget`
  (fake monotonic clock + injected connection prove the connect factory got
  the full 10.0s budget while request/header and each repeated body read
  got only the decreasing remainders 7.0/3.0/1.0 from the same budget, and
  budget exhaustion mid-reads is `AMBIGUOUS`),
  `test_deadline_expiry_after_connect_before_request_is_proven_unsent`
  (`RETRYABLE_FAILURE`/`DEADLINE_EXHAUSTED_PRE_DISPATCH`, zero request
  bytes),
  `test_post_dispatch_deadline_expiry_is_ambiguous`,
  `test_body_read_timeout_after_dispatch_is_ambiguous`.
- **F4 status truthfulness**:
  `test_status_pointer_is_current_after_delivered_run`,
  `test_status_pointer_missing_is_reported_and_not_repaired` (read-only),
  `test_status_pointer_stale_after_attempt_is_repairable` (status stays
  read-only; the mutating path then repairs byte-identically),
  `test_status_pointer_corrupt_noncanonical_foreign_contradictory_fail_closed`,
  `test_status_reports_unresolved_dispatch_claim_read_only` (orphan
  evidence projected, pointer untouched),
  `test_cli_delivery_status_corrupt_pointer_exits_two`.

Final local verification passed:

```text
python3 -m ruff check .                                  -> passed
python3 -m turtle_value_engine config validate
  --input config/project.example.toml                   -> valid
python3 -m pytest tests/test_monitoring_delivery.py     -> 62 passed
python3 -m pytest tests/test_monitoring_runner.py
  tests/test_monitoring_cycle.py
  tests/test_monitoring_cli.py                           -> 79 passed
python3 -m pytest                                        -> 6655 passed, 2 skipped
python3 scripts/export_surface_openapi.py --check       -> passed
pnpm --dir apps/dashboard api:check                     -> passed
pnpm --dir apps/dashboard types:check                   -> passed
pnpm --dir apps/dashboard lint                          -> passed
pnpm --dir apps/dashboard typecheck                     -> passed
pnpm --dir apps/dashboard test --run                    -> 44 passed
pnpm --dir apps/dashboard build                         -> passed
pnpm --dir apps/dashboard security:client-bundle        -> passed (CI sentinel values)
pnpm --dir apps/dashboard exec wrangler deploy --dry-run
  --config wrangler.jsonc --strict                     -> passed
python3 scripts/dashboard_cross_stack_smoke.py          -> passed
systemd-analyze verify deploy/monitoring/turtle-value-monitor.service
  deploy/monitoring/turtle-value-monitor.timer          -> passed
```

(The environment has no `python` executable; the equivalent `python3` was
used throughout, per the goal's executable-equivalence rule.)

## Manual intervention boundary

**NONE.** R1 has no planned manual verification. Every acceptance criterion
was proven automatically with subprocess barriers, injected monotonic
clocks/connections, injected lock failures and loopback/injected transports.
No owner webhook, token, VPS, systemd deployment, public endpoint,
screenshot or log inspection was required or performed.

## Exact closing CI evidence

```text
closing implementation SHA: <pending push>
Actions run ID: <pending>
Actions result: <pending>
```

This record flips to **CLOSED** only when the GitHub Actions run whose exact
`head_sha` equals the implementation SHA completes with conclusion `success`.
If Actions cannot be queried from this environment, the single remaining
lookup is `gh run list --commit <implementation-sha>` (or the equivalent
Actions API call) and the expected pass condition is a completed `success`
run for that exact SHA.

## Exact changed-file scope (12 files)

```text
schemas/monitoring-delivery-attempt.schema.json
schemas/monitoring-delivery-dispatch-claim.schema.json
schemas/monitoring-delivery-state.schema.json
src/turtle_value_engine/cli.py
src/turtle_value_engine/monitoring_delivery/__init__.py
src/turtle_value_engine/monitoring_delivery/contracts.py
src/turtle_value_engine/monitoring_delivery/locking.py
src/turtle_value_engine/monitoring_delivery/service.py
src/turtle_value_engine/monitoring_delivery/store.py
src/turtle_value_engine/monitoring_delivery/transport.py
tests/test_monitoring_delivery.py
docs/status/phase-6-d2b-r1-2026-09-23.md
```

(Plus the goal/roadmap/AGENTS status pointers updated by this commit.)
Test counts: `tests/test_monitoring_delivery.py` contributes 62 targeted
deterministic cases (43 carried over + 19 new); the full suite went from
6636 to 6655 passing tests with the same 2 opt-in live-AKShare skips.

## Stop boundary

No Phase 6-D3 Dashboard monitoring views, no Phase 6-E owner live
deployment/webhook acceptance, no Slack/Telegram/email adapter, no scheduled
monitoring GitHub Actions workflow, no FQGate Bridge adapter, no CNINFO
taxonomy/source expansion, no Cloudflare mutation, no brokerage/trading
operation, and no `strict-v1`/valuation/hard-gate/adjustment-approval/
monitoring-cursor/re-analysis change was started.
