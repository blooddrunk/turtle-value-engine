# Phase 6-E — owner live unattended acceptance — 2026-09-24

Status: **READY_FOR_OWNER_AUTHORIZED_PHASE_6E**

Implementation SHA:
`7a5da1996c53bcd77bbccb3c05ccd14a5e87e50f`

Closing CI: Actions run `35955876489`, `success`, `headSha`
exactly `7a5da1996c53bcd77bbccb3c05ccd14a5e87e50f`
(6719 passed / 2 skipped; monitoring glob, Dashboard gates,
generated-contract checks, client-bundle secret scan, strict Wrangler
dry-run, reference-unit `systemd-analyze verify` and the cross-stack smoke
all green).

Canonical goal: `docs/goals/phase-6-e-owner-live-unattended-acceptance.md`

Automation surface: `scripts/monitoring_live_acceptance.py` (modes `gate`,
`preflight`, `render`, `apply`, `verify`, `live-smoke`, `disable`,
`report`), checked-in example inputs
`config/monitoring-acceptance.example.json`, deterministic tests in
`tests/test_monitoring_live_acceptance.py` (24 cases: determinism, redaction,
secret refusal, gate binding, marker payloads, lock-visibility proofs,
socket guard, privilege simulation, report validation).

Superseded pushes (fail-closed, fixed before the final SHA):
`37cdaaff` (run `35955115979`, failure) and `5243280` (run `35955252682`,
failure) exposed one real harness defect: on passwordless-sudo CI hosts the
apply-privilege probe bypassed the injected runner and a fake install
crashed unclassified. The final SHA threads the runner through the
privilege probe and classifies "install reported success but the file does
not exist" as a fail-closed error, with a dedicated regression test.

## Host and topology

- Linux (kernel `6.18.33.2-microsoft-standard-WSL2`), `systemd 259` as
  PID 1 (`is-system-running=running`), Python `3.14.4` at
  `/usr/bin/python3` (editable install resolves the package without shell
  environment), `/proc/locks` readable.
- Deployment scope exercised: **user-level systemd units**
  (`~/.config/systemd/user/tve-phase6e-acceptance.{service,timer}`),
  installed and enabled by `apply` without privilege. A system-scope
  installation remains an owner production choice (the harness renders and
  verifies it and stops at the exact `MANUAL_SUDO_INSTALL_REQUIRED`
  commands when privilege is missing). `Linger=no` on this host means a
  user timer runs while the owner session exists; production persistence
  may want `loginctl enable-linger` (one privileged owner action).

## Lock-visibility proof (D3-R1 assumption on the real host)

Proven with a private acceptance lease slot, a real `RunnerLease.held()`
holder subprocess (the authoritative runner code path), the read-only
`tve watch unattended-status` CLI as the D3/status observer, and a second
runner-context subprocess:

```text
observer_while_held          = LIVE     (passive /proc/locks FLOCK)
runner_context_while_held    = BUSY     (authoritative held() contention)
observer_after_release       = ABANDONED
runner_context_after_release = ACQUIRED
observer_acquired_lock       = false    (the observer never locks)
```

The runner and D3/status observation contexts on this deployment host see
the same authoritative kernel flock truth; `DEPLOYMENT_LOCK_VISIBILITY_UNPROVEN`
was not triggered.

## Bounded live acceptance (machine-read report)

Secret-free report:
`.tve-private/monitoring/phase6e/root/acceptance-report.json`
(contract `monitoring_live_acceptance_report_v1`, bound to the
implementation SHA and the green gate artifact for the same SHA; an
earlier attempt-1 root is archived at
`.tve-private/monitoring/phase6e/root-attempt1-6532a877/`).

- Workspace: isolated `.tve-private/monitoring/phase6e/root`, watchlist
  `phase6e-live-acceptance` with `SH600519` + `SZ000858`, fixed historical
  CNINFO window `2026-04-15..2026-04-17` (contains the known Moutai FY2025
  annual-report disclosure cluster), explicit PIT `as_of`
  `2026-04-18T00:00:00Z`, runner `phase6e-owner-acceptance`.
- Live acquisition (`--network allow`, Phase 6-B bounded limits): canonical
  batch `a9892c80d463f0e916bb8a918f7af203a243cc6294483173dd5b915f58bde1dd`,
  25 events (`ANNUAL_REPORT` 3, `INFORMATIONAL_DISCLOSURE` 22) — byte-equal
  to the Phase 6-B live evidence batch of 2026-09-21 — raw payload only in
  the private acceptance cache.
- Socket-blocked offline replay: `--from-cache` rerun under a hard
  Python-level connect/getaddrinfo guard (`unshare --net` is not permitted
  on this host, recorded as such) produced byte-identical canonical output
  with zero connect attempts.
- Cursor semantics: no committed state before or after acquire-only; the
  cursor/state appeared only after the first D1 cycle commit
  (`state_id 1652d640…`, run `e60e82b8…`) and stayed byte-stable on rerun.
- Runner: first unattended run `COMPLETED_NEW`
  (activation `53a305bcf95ed4a59dd1e3d9beeb084f744bb4d89988973f77024e9639a5e7a7`,
  D1 `ATTENTION_REQUIRED` with blocked re-analysis jobs — the fail-closed
  no-execution-binding path, alerts emitted); immediate rerun
  `COMPLETED_REUSED` with identical activation/cycle identities and an
  unchanged cycle store.
- Crash/resume: SIGKILL of a live invocation observed after its durable
  intent file appeared; the kernel released the lease (probe `ABANDONED`);
  the retry returned `COMPLETED_RESUMED` for the same activation id with
  the same frozen PIT — no second activation was manufactured.
- systemd cadence: harness-started user timer fired at `12:33:00 CST`
  (ExecMainStartTimestamp evidence), journal shows the full
  `COMPLETED_REUSED` payload from the unit (exit 0, zero duplicate work),
  no unfinished activations; the timer was disabled afterwards by `disable`
  (units remain installed; re-enable is one owner command).
- Delivery (`tve watch deliver --network allow`, generic webhook-v1 only):
  `delivery_id b9dfc4dd8f8f7a48d5804b33183fa49258fd0733551759c3de911faa427b471b`,
  state `DELIVERED`, `attempt_count 1`, `dispatch_claim_count 1`,
  unresolved claims `[]`, pointer `CURRENT`. Re-invoking the same delivered
  identity: `reused=true`, `http_requests=0`, attempt count unchanged. The
  receiver journal matched the `Idempotency-Key` exactly once.
- D3 operations API (real `surface serve` with the acceptance runner
  config + acceptance project config): HTTP 200, contract-valid projection,
  activation matches the terminal receipt, lease state `ABANDONED`,
  `POST/PUT/PATCH/DELETE` all `405`, three repeated polls changed zero file
  bytes, the delivered identity is projected, and the payload contains no
  endpoint URLs, secrets, local roots or holder tokens.
- Secret scan: 45 generated artifacts scanned against every resolvable
  secret reference; zero hits. The report persists under the ignored
  private tree only.

## Owner boundary (the single marker crossed)

`MANUAL_SECRET_REFERENCE_REQUIRED` — the owner webhook endpoint reference
`TVE_MONITORING_WEBHOOK_URL` (and optional `TVE_MONITORING_WEBHOOK_TOKEN`)
was not resolvable in this environment, so delivery was proven against the
harness-local HTTPS receiver (ephemeral harness-generated certificate
trusted only inside the delivery subprocess). Exactly as required:

1. command/state before the stop: live-smoke delivery step completed
   against the local receiver with `delivery_id b9dfc4dd…` `DELIVERED`,
   because the endpoint reference resolved to nothing;
2. exact human action: make the owner webhook endpoint available under
   `TVE_MONITORING_WEBHOOK_URL` (optionally the bearer token under
   `TVE_MONITORING_WEBHOOK_TOKEN`) in the approved secret
   manager/environment — without revealing either value — and, in
   `.tve-private/monitoring/phase6e/acceptance-config.json`, set
   `delivery_receiver` to `owner_env` (refusing the harness-local receiver)
   and change `delivery_destination_id` to a fresh value such as
   `phase6e-owner-primary` (keeping the old destination would resolve to the
   already-`DELIVERED` ledger identity and authorize zero outbound
   requests);
3. secret boundary: both values must stay out of Git, committed documents,
   chat and this report;
4. exact resume command:
   `python3 scripts/monitoring_live_acceptance.py --acceptance-config .tve-private/monitoring/phase6e/acceptance-config.json live-smoke --network allow --allow-existing`
   (the pre-existing workspace then proves `COMPLETED_REUSED`/cursor
   stability and skips the destructive crash injection already proven in
   the fresh run);
5. machine-verifiable success: `tve watch deliver ... --network allow`
   returns `DELIVERED` for the owner endpoint, `delivery-status` reports
   pointer `CURRENT`, and the repeated identity authorizes zero additional
   dispatch slots;
6. remaining unverified if the owner stops there: delivery of the
   acceptance alert outbox to the owner's real destination (all D2B/R1/R2
   ledger mechanics were already proven against the local receiver).

Phase 6-E stays **READY_FOR_OWNER_AUTHORIZED_PHASE_6E** (not CLOSED) until
that owner-authorized delivery lands; every other machine-verifiable
boundary above is green on the real host at the exact implementation SHA.

## Stop boundary

No vendor-specific notification adapters, scheduled GitHub Actions
monitoring, Bridge/FQGate expansion, M4-D/M4-E/M2-D, brokerage/trading
operations or investment-rule changes were started. The recurring
acceptance timer is disabled; nothing on the host keeps mutating.
