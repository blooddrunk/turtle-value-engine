# Phase 5R-A audit and next package — 2026-09-19

Status: **M6-A AUDITED COMPLETE; M6-B COMPLETE; M5-A/B COMPLETE; A6 PENDING**

## 1. M6-A audit

Audited `main` at `ad864510fd2beaa9d5c3471128199c2b1f91efb3`
(`feat: add offline research surface contract`).

The implementation adds:

- typed/versioned `ResearchSurfaceSnapshotV1` contracts;
- checked-in JSON Schema;
- explicit allowlisted projection code;
- analysis/trace/report identity and `as_of` fail-closed checks;
- historical dataset/readiness/acceptance/validation compatibility checks;
- deterministic snapshot/content identity;
- offline `tve surface build` / `validate`;
- focused schema/identity/leakage/CLI tests.

The implementation boundary remains read-only and does not alter
`strict-v1`, deterministic calculations, historical acquisition, M4/M5 or A6
semantics.

No structural defect was found that requires reopening M6-A.

## 2. Verification evidence and limitation

The repository's M6-A closure record states:

```text
python3 -m ruff check .: PASS
python3 -m pytest tests/test_research_surface.py -q: 10 passed
python3 -m pytest: 6286 passed, 2 skipped
```

GitHub exposes no attached commit status or workflow run for `ad864510`.

This audit also attempted an independent clone/test run, but the audit container
could not resolve `github.com`:

```text
git clone --depth 1 https://github.com/blooddrunk/turtle-value-engine.git ...
fatal: unable to access ...: Could not resolve host: github.com
```

Therefore the repository-recorded test counts are **not** represented as an
independent rerun by this audit. The code/contract audit is independent; the
test execution evidence remains the recorded implementation run.

This limitation is not an M6-A code blocker, but it directly motivates the
M6-B requirement that Codex execute and record all automatable verification
before claiming closure.

## 3. Documentation drift corrected

Before this audit, two source-of-truth/parent documents still pointed at an old
next-stage state even though M6-A was already complete:

- `AGENTS.md`;
- `docs/goals/phase-5r-a-local-research-and-low-cost-sources.md`.

The roadmap also left the post-M6-A package intentionally unselected.

This audit selects and freezes M6-B as the next package and updates the active
handoff accordingly.

## 4. M6-B closure and exact verification

M6-B implements the explicit-input, read-only boundary described by the active
goal:

- `SurfaceRegistry` and `SurfaceReadService` validate explicit in-memory
  snapshots or repeatable explicit paths, reject empty/tampered/duplicate
  registrations, keep deterministic ordering/filtering, and never discover
  workspace/raw-store files;
- the optional `api` extra contains lazy FastAPI/uvicorn/httpx dependencies;
- the API exposes only `GET /healthz`, `GET /v1/surfaces` and
  `GET /v1/surfaces/{surface_id}`, with stable error codes, ETag/304 handling,
  no mutation routes, and loopback-first serving;
- `tve surface serve` defaults to `127.0.0.1`; non-loopback binding requires
  `--allow-non-loopback`;
- `tests/test_surface_api.py` includes the automated real subprocess/socket
  smoke: it waits for `/healthz`, reads a known surface and verifies its
  `surface_id`/`content_sha256`, checks an unknown ID and mutation rejection,
  then sends SIGINT and asserts clean server exit.

Dependency setup was initially blocked by PEP 668, then succeeded using the
same repository-declared extra with the environment's explicit override:

```text
python3 -m pip install -e '.[api,dev]'
exit 1
error: externally-managed-environment

python3 -m pip install --break-system-packages -e '.[api,dev]'
exit 0
Successfully installed fastapi-0.141.1 httpx-0.28.1 uvicorn-0.53.0 ...
```

The required pre-edit baseline ran before implementation:

```text
python3 -m ruff check .
exit 0: All checks passed!

python3 -m pytest
exit 0: 6286 passed, 2 skipped in 11.14s
```

The required post-implementation gates passed:

```text
python3 -m ruff check .
exit 0: All checks passed!

python3 -m pytest tests/test_research_surface.py -q
exit 0: 10 passed in 0.28s

python3 -m pytest tests/test_surface_api.py -q
exit 0: 8 passed in 1.08s

python3 -m pytest tests/test_surface_api.py::test_real_loopback_socket_smoke_starts_cli_server_and_shuts_down_cleanly -q
exit 0: 1 passed in 0.84s

python3 -m pytest
exit 0: 6294 passed, 2 skipped in 12.47s
```

The two full-suite skips remain the pre-existing opt-in live AKShare tests:
`tests/test_akshare_live.py:22` and `tests/test_akshare_live.py:28`.
`BLOCKED_BY_EXECUTION_ENVIRONMENT` does not apply: package installation,
process spawning and loopback binding all succeeded, and the real smoke is
included in the focused API count above.

## 5. Scope after M6-B

M6-B is closed. M5-C remains optional for a concrete remote artifact-class
need; M6-C Worker/Dashboard, authentication/deployment integration and Phase 6
watchlist/event monitoring remain later packages and were not started.

## 6. Post-M6-B audit and next-package selection — 2026-09-19

A post-implementation audit inspected `main` at
`dda3a0dcfd15f18f8ac2aa19664a08b27d960736`
(`feat: add read-only research surface API`) against the M6-B goal.

The implementation matches the documented boundary:

- explicit-input `SurfaceRegistry` and detached read service;
- startup validation and duplicate/tamper rejection;
- deterministic metadata filters/order;
- lazy optional FastAPI/uvicorn adapter;
- `/healthz`, `/v1/surfaces`, detail, ETag/304 and stable errors;
- loopback default plus explicit non-loopback opt-in;
- mutation/path-traversal rejection tests;
- a real subprocess/socket smoke that starts and shuts down the CLI server.

The M6-B closure record contains the implementation-environment automated
results: ruff green, focused M6-A/API tests green, the real loopback smoke green,
and full pytest at `6294 passed, 2 skipped`. The two skips are the pre-existing
opt-in live AKShare tests.

GitHub did not expose an Actions/status-check run for this commit, so there is no
second CI result to cite. A separate audit-container attempt to clone and rerun
the repository failed before checkout with
`Could not resolve host: github.com`; therefore this audit does **not** claim an
independent second full-suite execution. That audit-environment DNS failure does
not alter the already recorded M6-B implementation evidence.

No M6-B defect requiring reopening was found in the reviewed registry/API/CLI
and test boundary.

The selected next package is **M6-C1 — Personal Dashboard Foundation**:
`docs/goals/phase-5r-a-m6-c1-personal-dashboard-foundation.md`.

M6-C1 is intentionally local/preview-first so Codex can automatically prove the
Python API + Worker proxy + React application integration before any owner cloud
setup. Live Cloudflare deployment, authentication and remote-origin security are
deferred to M6-C2. M5-C remains optional; Phase 6 monitoring remains later.

