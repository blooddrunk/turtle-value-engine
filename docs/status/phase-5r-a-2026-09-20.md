# Phase 5R-A audit and next package — 2026-09-20

Status: **M6-C1 COMPLETE; M6-C2 CLOSED / OWNER_ACCEPTED; M6-C3 SELECTED / NOT_STARTED; A6 PENDING**

Current code/documentation verification commit:
`93c638fd1231e904fab50b3aeb21eab7e6e41874`. GitHub Actions run
`35496754597` for this commit is green; its `test` job and all 20 execution
steps passed, including project runtime config validation.

## 1. M6-C1 implementation audit

The published M6-C1 implementation was inspected on GitHub rather than accepted
from the closure prose alone.

Confirmed in the checked-in tree:

- React 19 / TypeScript / Vite Dashboard under `apps/dashboard`;
- TanStack Router + Query read-only list/detail flow;
- generated OpenAPI contract and generated TypeScript surface types;
- bounded same-origin Worker proxy;
- GET/HEAD-only Worker behavior;
- server-side `SURFACE_API_ORIGIN`;
- explicit query/path allowlists;
- POST/PUT/PATCH/DELETE mutation rejection with
  `READ_ONLY_METHOD_NOT_ALLOWED`;
- DOM coverage for `PARTIAL`, `BLOCKED`, `NOT_AVAILABLE` and
  `NOT_EVALUATED`;
- real `scripts/dashboard_cross_stack_smoke.py` that starts the actual M6-B
  server and Cloudflare/Vite preview and verifies health/list/detail/404, all
  four mutation methods and clean shutdown.

The implementation merge is `5774b297ab2af5595d24507f12c2d207004a4789`.

## 2. CI defect discovered by this audit

The previous closure documentation stated local automated gates were green, but
GitHub Actions on the post-M6-C1 `main` commits was actually red.

For run `35423552418` on `2e976be182755025268842a3d871b430368633c0`:

- dependency install used only `".[dev]"`;
- Ruff passed;
- full pytest ended `2 failed, 6291 passed, 3 skipped`;
- both failures were M6-B API tests raising
  `SurfaceAPIUnavailableError` because FastAPI was not installed;
- the API subprocess smoke was skipped because `httpx` was unavailable.

This was a CI dependency/coverage defect rather than evidence of a failing
Dashboard implementation, but a red default-branch CI could not be ignored as
“no CI evidence”.

## 3. CI remediation and independent verification

The audit updated `.github/workflows/ci.yml` directly on `main` in:

`8446b47c936fdbac041ec34265c4b666661780ac`
(`ci: verify API and dashboard integration`).

The workflow now:

- installs `.[api,dev]`;
- runs Ruff and the full Python suite;
- installs Node 24 and pnpm 11.23.0;
- performs frozen Dashboard dependency install;
- checks deterministic OpenAPI export;
- checks generated Dashboard API types;
- runs Dashboard lint, typecheck and tests;
- runs production build;
- runs the real Dashboard cross-stack smoke.

GitHub Actions run `35482225704` for that exact CI-hardening commit completed
with `success`. This provides independent default-branch CI evidence for the
M6-B/M6-C1 Python API and Dashboard integration gates instead of relying only on
the implementation environment's recorded local results.

The later commits in this audit are documentation-only planning/status updates
and use the same hardened workflow. Before M6-C2 implementation begins, Codex
must still inspect the latest current-`main` Actions run and require it to be
green.

## 4. Next package selection

The selected next package is:

**M6-C2 — Private Dashboard Deployment and Authenticated Origin**

Goal:
`docs/goals/phase-5r-a-m6-c2-private-dashboard-deployment.md`.

M6-C2 hardens remote-origin handling, adds server-side Access service-token
authentication, deploys/protects the Dashboard, establishes a secure private
M6-B origin path (Cloudflare Tunnel to loopback is preferred), and proves the
live path with repeatable smoke tests.

M5-C/R2 remains deferred by default because a persistent M6-B origin behind a
secure Tunnel can serve the existing read model directly. Phase 6 event
monitoring remains a later package.

## 5. Verification policy for the next package

The M6-C2 implementation now automates every machine-verifiable check that is
possible without owner-specific Cloudflare and origin-host inputs. The exact
remaining live inputs and commands are recorded below.

Owner intervention is limited to real account/identity facts that Codex cannot
invent, such as Cloudflare authorization, owner-selected hostnames/identity
policy, access to the origin host, explicit snapshot paths and one final
interactive identity-provider login check.

The M6-C2 goal contains the exact owner input list, automated deployment duties,
live smoke requirements, manual verification sequence and explicit blocked
states. Vague statements such as “manual verification recommended” or
“evidence incomplete” are not acceptable closure evidence.

## 6. M6-C2 implementation

Implemented on the synchronized `main` baseline after green run
`35482336396` for `a54f86b60f111e7dca9c5bb6a3cbcb903bfeaabf`:

- `apps/dashboard/worker/index.ts` now accepts local loopback HTTP only for
  preview; remote origins require bare HTTPS and a complete server-side
  `SURFACE_API_ACCESS_CLIENT_ID` / `SURFACE_API_ACCESS_CLIENT_SECRET` pair;
  malformed/userinfo/path/query/fragment origins fail closed;
- the Worker constructs Access headers server-side, ignores browser credential
  headers, forwards only `If-None-Match`, keeps the `/api/*` allowlist and
  rejects mutations before `fetch`;
- `apps/dashboard/wrangler.jsonc` explicitly sets `workers_dev: false`,
  `preview_urls: false`, observability, a loopback default origin and the
  generated `ASSETS` binding/types;
- `scripts/dashboard_deploy.py` performs fail-closed input preflight, build and
  Wrangler dry-run, then explicit `--apply` Cloudflare API/Wrangler
  Access/Tunnel/DNS/secret operations and resource verification. Its Access
  Service Auth policies use Cloudflare's API decision value `non_identity`;
  the user-facing policy concept remains service authentication;
- `scripts/dashboard_live_smoke.py` is the repeatable live chain probe;
- non-secret production, Access-policy and loopback Tunnel examples are under
  `deploy/dashboard/`.

M5-C/R2 remains explicitly deferred. The persistent authenticated M6-B origin
serves existing validated snapshots directly; no artifact mirroring was needed.
No strict-v1, PIT/A6, source-selection, investment math, Phase 6, M4-D/M4-E or
M2-D work was started.

## 7. Automated verification record

The following commands were executed in this task. Exit codes are shown
explicitly; generated build output and ignored `dist/` files are not source
artifacts.

| Command | Result |
| --- | --- |
| `git fetch origin main --prune` + `git pull --ff-only origin main` | exit 0; fast-forwarded `2e976be` to `a54f86b` |
| `gh run view 35482336396 --json ...` | exit 0; current-main CI `success`; `test` job and all 16 execution steps succeeded |
| `python3 -m ruff check .` (pre-edit and post-edit) | exit 0; all checks passed |
| `python3 -m pytest` (pre-edit) | exit 0; `6294 passed, 2 skipped` |
| `python3 -m pytest tests/test_research_surface.py -q` | exit 0; `10 passed` |
| `python3 -m pytest tests/test_surface_api.py -q` | exit 0; `8 passed` |
| `python3 -m pytest tests/test_dashboard_deployment.py -q` | exit 0; `4 passed` |
| `pnpm --dir apps/dashboard install --frozen-lockfile` | exit 0; pnpm `11.23.0`, already up to date |
| `pnpm --dir apps/dashboard types:check` | exit 0; generated Wrangler types current |
| `pnpm --dir apps/dashboard lint` | exit 0 |
| `pnpm --dir apps/dashboard typecheck` | exit 0 |
| `pnpm --dir apps/dashboard test --run` | exit 0; `2 files, 14 tests passed` including 7 M6-C2 Worker security tests |
| `pnpm --dir apps/dashboard build` | exit 0; Worker and client production build succeeded |
| `python3 scripts/export_surface_openapi.py --check` | exit 0; OpenAPI deterministic/current |
| `pnpm --dir apps/dashboard api:check` | exit 0; generated Dashboard API types current |
| `python3 scripts/dashboard_cross_stack_smoke.py` | exit 0; fixture → loopback M6-B → Worker/Vite preview health/list/detail/404/mutation and shutdown passed |
| `python3 scripts/dashboard_deploy.py deploy --dry-run` with ephemeral non-secret validation inputs | exit 0; Wrangler `4.134.0` read 5 assets and exited at `--dry-run` without cloud mutation |
| `python3 scripts/dashboard_deploy.py preflight` with owner inputs absent | exit 2; listed every missing hostname, paired credential, Cloudflare account/token, Tunnel/zone and snapshot input, without mutation |
| `python3 -m py_compile scripts/dashboard_deploy.py scripts/dashboard_live_smoke.py` | exit 0 |
| `python3 scripts/dashboard_live_smoke.py` without live inputs | exit 1; fail-closed at `TVE_DASHBOARD_URL is required` |
| `command -v cloudflared` | no executable present in this environment |
| Tunnel example YAML parse + loopback/terminal-404 assertion | exit 0 |

After the implementation commit was pushed, GitHub Actions run
`35484472809` for `41da846ecb3e7b2835933c0c9d9a8bd463eba7f4` completed with
`success` (job `test`, all required execution steps successful). It reran the
Python dependency install, Ruff, full deterministic suite, frozen pnpm
Dashboard install, OpenAPI and generated Wrangler/API drift checks, Dashboard
lint/typecheck/Vitest/build, and the real cross-stack smoke. The only run
annotations were GitHub's runner notices about Node 20 action deprecation and
the future `ubuntu-latest` image migration; neither failed a job or step.

Cloudflare's Access API documentation was then cross-checked before live use:
the Service Auth policy concept is represented by the API decision value
`non_identity`. Follow-up commit
`43bfa8f7e5191266dcec2635fd602d5fd82d9808` corrected the deployment helper,
policy template and regression test. GitHub Actions run `35484921932` for that
commit completed with `success`; its `test` job and every required step passed.

The pre-edit Dashboard gates also passed: Node `v24.19.0`, frozen pnpm
install, lint, typecheck, Vitest `7 passed`, build, OpenAPI drift, generated
API drift and the real local cross-stack smoke.

## 8. Final hardening and current-main CI

The final deployment audit added one more fail-closed validation layer and
closed two configuration/documentation defects:

- the checked-in source Wrangler config now declares its asset directory, and
  CI runs `wrangler deploy --dry-run --config wrangler.jsonc --strict` after
  the Dashboard build;
- production verification requires exactly the intended Dashboard and origin
  hostnames, exact Access policy decision sets, the expected service-token IDs,
  and the selected owner identity; colliding hostnames, ambiguous Tunnel
  inputs, whitespace credentials and path-subtree Access applications fail;
- live smoke now sends invalid origin service credentials before the valid
  request, so an origin that merely checks for the presence of headers cannot
  pass;
- the deployment wrapper now passes the API token and both service-token pairs
  into the subprocess redaction set before the first Wrangler deploy, with a
  regression test covering both stdout and stderr;
- the verifier now requires the Dashboard zone ID and lists its zone-level
  Worker routes before apply/verify, failing closed on incomplete pagination
  or any route still attached to `tve-personal-dashboard`;
- the deployment README uses the current Cloudflare CLI ordering for
  `cloudflared tunnel --config ... ingress validate`.

Before this hardening, current `main` was
`ece4610eb61f1c5340ceb0ad3d6b727ad9fd53d1`; run `35485017083` was green.
The hardening commit is
`3c8e188ac58f05b11a41389d1041181190ca24a1`. GitHub Actions run
`35485721496` for that exact SHA completed with `success`; its `test` job and
all 18 execution steps passed, including the new source Wrangler deployment
configuration dry-run.

The secret-redaction follow-up is
`297d7cf1232823fc3dab1b5758dae8d6907275d2`. GitHub Actions run
`35486605298` for that exact SHA completed with `success`; its `test` job and
all 18 execution steps passed.

The route-isolation follow-up is
`a68fa3196f496d08782d01cc2ea9bf5823245f25`. GitHub Actions run
`35487136705` for that exact SHA completed with `success`; its `test` job and
all 18 execution steps passed.

The route-pagination regression-test follow-up is
`65f413c4a62ecd9adc8c531a42f372f7504c62b5`. GitHub Actions run
`35487290954` for that exact SHA completed with `success`; its `test` job and
all 18 execution steps passed.

The complete-list fail-closed follow-up is
`4c6523c2d8f21e4f44c4aee4304501311a7ab44c`. GitHub Actions run
`35487651724` for that exact SHA completed with `success`; its `test` job and
all 18 execution steps passed. Cloudflare service-token, Access-app, Worker
domain, DNS-record and Worker-route list responses now require matching
pagination metadata before apply/verify proceeds.

The read-only method-coverage follow-up is
`7859d7021726e1ea09e80126e3f30e844f735015`. It extends the Worker security
test, M6-B loopback tests, local cross-stack smoke and repeatable live smoke to
exercise POST, PUT, PATCH and DELETE; every method must be rejected before any
upstream fetch. GitHub Actions run `35488158752` for this exact SHA completed
with `success`: the single `test` job and all 18 execution steps passed,
including the full Python suite, frozen Dashboard install, generated-contract
checks, Dashboard lint/typecheck/Vitest/build, source Wrangler dry-run and
real cross-stack smoke. The run emitted only GitHub's Node 20 deprecation and
future `ubuntu-latest` migration notices.

The follow-up local results were:

| Command | Result |
| --- | --- |
| `python3 -m ruff check .` | exit 0; all checks passed |
| `python3 -m pytest tests/test_research_surface.py tests/test_surface_api.py -q` | exit 0; `18 passed` |
| `python3 -m pytest` | exit 0; `6303 passed, 2 skipped` |
| `pnpm --dir apps/dashboard install --frozen-lockfile` | exit 0; pnpm `11.23.0`, already up to date |
| Dashboard lint/typecheck/types:check/Vitest/build | exit 0; Vitest `14 passed` and production build succeeded |
| `python3 scripts/export_surface_openapi.py --check` + Dashboard `api:check` | exit 0; generated contracts current |
| `pnpm --dir apps/dashboard exec wrangler deploy --dry-run --config wrangler.jsonc --strict` | exit 0; source config read 5 client assets without cloud mutation |
| `python3 scripts/dashboard_cross_stack_smoke.py` | exit 0; real local chain passed health/list/detail/404, POST/PUT/PATCH/DELETE 405 and clean shutdown |
| `python3 scripts/dashboard_deploy.py preflight` with owner inputs absent | exit 2; fail-closed list of missing owner inputs |
| `python3 scripts/dashboard_live_smoke.py` without live inputs | exit 1; fail-closed at `TVE_DASHBOARD_URL is required` |
| `wrangler whoami` / `command -v cloudflared` | Wrangler reported not authenticated; cloudflared was not on PATH; no live apply was attempted |

The current fail-closed deployment follow-up is
`8b930dff8ab289b67a9c02fc7e92de7564462d1f`. It makes the apply preflight
require complete pagination metadata for every alternate-ingress list before
any Cloudflare mutation; requires workers.dev and preview ingress to be
explicitly false; rejects existing extra Worker custom domains, zone routes,
path/wildcard/duplicate Access applications and Access apps overlapping the
Dashboard and origin hostnames. Post-deploy verification uses the same
Access-target and explicit subdomain checks. Live smoke credentials now also
reject whitespace-only and surrounding-whitespace values. GitHub Actions run
`35489083702` completed successfully with all 18 execution steps, including
the full Python suite and Dashboard checks. The latest local Python results
were `6306 passed, 2 skipped`; deployment plus live-smoke regression tests
were `12 passed`.

The service-token symmetry follow-up is
`3fb2bf7e9706ac9e8e054160a45259d234c1ba7d`. The Worker security test now
explicitly covers both half-configured states: client ID without secret and
secret without client ID; neither reaches upstream fetch. GitHub Actions run
`35489453533` completed successfully with all 18 execution steps.

The live-smoke hostname-isolation follow-up is
`0ae71121138601b61694f2f2e1b1039a9620fda1`. Standalone live smoke now
rejects Dashboard and origin URLs that use the same hostname, matching the
deployment preflight collision rule. GitHub Actions run `35489756086`
completed successfully with all 18 execution steps.

The production-config secret-safety follow-up is
`c93814b3de6c034da77ea526cef1a3bfb1300026`. Its deployment regression test
serializes the generated production Wrangler config and asserts that the API
token and both Access service-token pairs are absent, while retaining the
explicit HTTPS origin variable, disabled workers.dev/preview ingress and exact
Dashboard custom-domain route. GitHub Actions run `35490033160` completed
successfully with all 18 execution steps. The focused deployment tests passed
(`12 passed`), the standalone live-smoke validation tests passed (`2 passed`),
and the full local deterministic suite passed (`6308 passed, 2 skipped`).

The malformed-origin-credential hardening follow-up is
`ce3956e1ceb04aff462228bc7747d5b686236a80`. The Worker now rejects empty,
whitespace-only and surrounding-whitespace Access credential values before any
upstream fetch for both loopback preview and remote HTTPS origins; its
deterministic security test covers each form. GitHub Actions run `35490794888`
completed successfully with all 18 execution steps. The Dashboard Vitest
suite passed (`15 passed`), the focused M6-A/M6-B/M6-C2 Python tests passed
(`32 passed`), and the full local deterministic suite passed (`6308 passed,
2 skipped`).

The client-bundle secret-safety follow-up is
`cdca47f291d3cbc3bac8f51933c62f7cd0e13408`. CI now builds the Dashboard with
test-only runtime secret sentinels and scans every generated `dist/client`
file without printing sentinel values; the scan passed. The repeatable local
command is `pnpm --dir apps/dashboard security:client-bundle`. GitHub Actions
run `35491132677` completed successfully with all 19 execution steps,
including the bundle scan. The local scan passed over 4 client files;
Dashboard Vitest remained `15 passed` and the full local deterministic suite
remained `6308 passed, 2 skipped`.

The documentation closure commit
`aed18ee54795406dfbe77b022f8c706c39d18d8d` was then pushed. Its
documentation-only GitHub Actions run `35486022150` also completed with
`success`; the `test` job passed all 18 execution steps.

The final local rerun after this hardening recorded:

| Command | Result |
| --- | --- |
| `python3 -m ruff check .` | exit 0 |
| `python3 -m pytest` | exit 0; `6303 passed, 2 skipped` |
| `python3 -m pytest tests/test_dashboard_deployment.py -q` | exit 0; `9 passed` |
| `python3 -m py_compile scripts/dashboard_deploy.py scripts/dashboard_live_smoke.py` | exit 0 |
| `pnpm --dir apps/dashboard install --frozen-lockfile` | exit 0 |
| `pnpm --dir apps/dashboard lint` | exit 0 |
| `pnpm --dir apps/dashboard typecheck` | exit 0 |
| `pnpm --dir apps/dashboard types:check` | exit 0 |
| `pnpm --dir apps/dashboard test --run` | exit 0; `2 files, 14 tests passed` |
| `pnpm --dir apps/dashboard build` | exit 0 |
| `python3 scripts/export_surface_openapi.py --check` | exit 0 |
| `pnpm --dir apps/dashboard api:check` | exit 0 |
| `pnpm --dir apps/dashboard exec wrangler deploy --dry-run --config wrangler.jsonc --strict` | exit 0; source config read the built client assets without cloud mutation |
| `python3 scripts/dashboard_deploy.py deploy --dry-run` with ephemeral non-secret inputs including `TVE_DASHBOARD_ZONE_ID` | exit 0; generated production config and Wrangler dry-run passed without cloud mutation |
| `python3 scripts/dashboard_cross_stack_smoke.py` | exit 0; real local M6-B → Worker/Vite chain passed |
| `python3 scripts/dashboard_live_smoke.py` without owner inputs | exit 1; fail-closed at `TVE_DASHBOARD_URL is required` |
| `cloudflared tunnel --config deploy/dashboard/tunnel-config.example.yml ingress validate` using the official temporary `2026.9.1` binary | exit 0; `Validating rules... OK` |
| `git diff --check` | exit 0 |

The temporary cloudflared binary was verified against the official release
SHA-256 before validating the checked-in loopback/terminal-404 Tunnel shape;
it was not added to the repository. No Cloudflare API/Wrangler apply, secret
upload, DNS mutation or live origin connection was attempted because the
owner-specific inputs in section 9 remain absent.

## 9. Historical READY_FOR_OWNER_AUTHORIZED_LIVE_ACCEPTANCE

The offline implementation is complete, but live acceptance cannot be honestly
claimed because these owner-specific inputs are absent from the execution
environment:

1. A custom Cloudflare API token named `tve-private-dashboard-deployer`, with
   the exact least-privilege account/zone permissions in
   `docs/operations/project-runtime-config.md`. Store its one-time value only
   as `CLOUDFLARE_API_TOKEN` in the owner-approved secret
   manager/environment; never paste it into chat or commit it.
2. Three non-secret project facts in `.tve-private/project.toml`: the
   Cloudflare root `zone_name`, the approved interactive
   `dashboard_access_email`, and the explicit validated `surface.snapshot_path`.
   Hostnames, account/zone IDs, Tunnel name and Access resource names are
   derived/discovered; they are not owner inputs unless an existing layout
   requires overrides.
3. A host authorized to run `tve surface serve` and `cloudflared`. The M6-B
   process remains bound to `127.0.0.1` only, and the connector token remains
   an environment/secret-manager reference.
4. Existing Access service-token pairs are optional. With the API token, the
   explicit `--create-origin-service-token --create-dashboard-service-token
   --live-smoke` path creates the named pairs and consumes their one-time
   secrets in memory without printing or persisting them.

Once those references exist, Codex will run exactly:

```bash
python3 scripts/dashboard_deploy.py preflight --project-config .tve-private/project.toml
python3 scripts/dashboard_deploy.py deploy --project-config .tve-private/project.toml --dry-run
python3 scripts/dashboard_deploy.py deploy --project-config .tve-private/project.toml --apply --create-tunnel --create-origin-service-token --create-dashboard-service-token --live-smoke
python3 scripts/dashboard_deploy.py verify --project-config .tve-private/project.toml
```

The deploy command automatically builds the exact checkout, checks Wrangler,
creates/updates only the named Access apps, remote Tunnel ingress, origin CNAME
and Worker secrets, verifies workers.dev/preview/custom-domain/bypass state,
and records only non-sensitive resource IDs/hostnames. The origin host runs:

```bash
python3 scripts/dashboard_deploy.py origin \
  --project-config .tve-private/project.toml
```

The live smoke then proves unauthenticated Dashboard/origin blocking and the
authenticated Dashboard → Worker → HTTPS origin → M6-B chain, including
health, list, known detail identity/hash, 404, mutation 405 and ETag 304.

Not yet passed, solely because of the missing inputs: live Cloudflare Worker
deployment, Access policies, Tunnel connector/ingress reachability, origin
unauthenticated blocking, authenticated live surface reads, alternate-route
checks against the real account, and the owner interactive IdP check.

After automated live smoke passes, the only remaining human acceptance is the
goal-defined incognito browser check: Access wall before Dashboard, approved
identity reads overview and one detail, and a fresh unauthenticated session
cannot see Dashboard content. This does not replace any automated check above.

## 10. Project-wide runtime configuration follow-up — 2026-09-20

The deployment input model was consolidated into the reusable project-level
configuration at `config/project.example.toml`, copied to the ignored
`.tve-private/project.toml`. It is not an M6-C2-only file: its versioned common
sections cover project identity, rule-profile selection, private paths, network
policy, Surface runtime, Cloudflare facts, provider/storage extension points and
Dashboard deployment names. Future phases must extend this file and its typed
loader rather than add another phase-local configuration convention.

`ProjectConfig` rejects raw credential fields, stores only secret references,
and `tve config validate` prints only a safe summary. M6-C2 deployment and live
smoke scripts consume this config, derive `tve-private-dashboard.<zone>` and
`tve-private-surface.<zone>`, resolve account/zone IDs with read-only API lookup, preserve
legacy environment-variable overrides and use these generated resource names:

- `tve-private-dashboard`;
- `tve-private-surface-origin`;
- `tve-private-dashboard-origin`;
- `tve-private-dashboard-smoke`.

An explicit `--create-origin-service-token --create-dashboard-service-token
--live-smoke` path consumes newly returned one-time service secrets in process
memory and refuses to print or persist them. The canonical token instructions
are in `docs/operations/project-runtime-config.md` and
`deploy/dashboard/README.md`.

The latest current-`main` baseline was independently checked after the
configuration work began: GitHub Actions run `35491302940` for
`5a8ed8a800ad04904f29e0b9dc63859e3540f770` was `success`; its `test` job and
all 19 execution steps passed, including Ruff, the full Python suite, frozen
Dashboard install, OpenAPI/generated-type drift checks, lint, typecheck,
Vitest, build, client-bundle secret scan, source Wrangler dry-run and local
cross-stack smoke. The configuration change is accepted by that post-push CI
run; at that pre-live checkpoint no Cloudflare mutation was attempted because
owner authorization inputs remained absent.

## 11. Owner-authorized live M6-C2 result — 2026-09-20

The previous `READY_FOR_OWNER_AUTHORIZED_LIVE_ACCEPTANCE` section is now
historical. The owner supplied `zone_name = "haoqi90.top"`,
`dashboard_access_email = "xieyh@outlook.com"`, the explicit snapshot path
`.tve-private/surface/research-surface.json`, and a Cloudflare API token through
the ignored private secret handoff. Codex created the named service tokens,
Access applications, remotely managed Tunnel, DNS record and Worker secrets
through Cloudflare API/Wrangler without printing or committing secret values.

Target artifact: `HK0288`, `as_of=2026-09-20`, validated surface identity and
content hash
`1564c885738b010148225e8f2aa84d198ea819fc517c4ffcf21fbc0649053862`, analysis
`analysis-18069e2f1b8f3cb9fb8d9d9c`, deterministic state `SPECIAL_REVIEW`.

Non-secret Cloudflare evidence:

| Resource | Actual value |
| --- | --- |
| Account / zone | `51eaede3a49980ea51dfe61d01cab7f7` / `9a7d331c8d376c528f9180dba7b1fc91` |
| Worker | `tve-personal-dashboard` |
| Dashboard | `https://tve-private-dashboard.haoqi90.top` |
| Origin | `https://tve-private-surface.haoqi90.top` |
| Tunnel | `0e19c8cc-1af4-48d9-8af5-d65717b56a6c` |
| Origin DNS record | `dd049985edcfd6082d518d58adcfb1d6` |
| Dashboard / origin Access apps | `715ee443-7eeb-4108-9a35-8ea8f2c15f3d` / `f4bedf6f-7a53-4787-a8fa-cc5d7957f152` |

The exact Tunnel ingress is `tve-private-surface.haoqi90.top` to
`http://127.0.0.1:8787`, followed by terminal `http_status:404`. M6-B remains
loopback-only. The Worker custom domain is production-enabled and workers.dev,
preview URLs and zone-level alternate Worker routes are disabled or absent.

The actual automated deployment and verification results are:

| Command / check | Result |
| --- | --- |
| `git fetch origin main --prune`; latest baseline CI | local `main == origin/main == 6f28fb034612cb9b1101268f6abd2468a3238e74`; Actions run `35500161280`, `success` |
| `python3 scripts/dashboard_deploy.py preflight --project-config .tve-private/project.toml` | exit 0 |
| `python3 scripts/dashboard_deploy.py deploy --project-config .tve-private/project.toml --dry-run` | exit 0; HTTPS production config, workers.dev/preview disabled |
| owner-authorized `deploy --apply --live-smoke` | exit 0; Worker/Access/Tunnel/DNS/secrets/resource verification and live smoke passed |
| `python3 scripts/dashboard_deploy.py verify --project-config .tve-private/project.toml` | exit 0 |
| `cloudflared tunnel --config deploy/dashboard/tunnel-config.example.yml ingress validate` | exit 0; `OK` |
| `python3 scripts/dashboard_live_smoke.py --project-config .tve-private/project.toml` | exit 0; auth, origin, Worker, M6-B, 404, all four mutations and ETag 304 passed |

The first live probe returned Cloudflare Error 1010 because the default
Python User-Agent was blocked by the zone's Browser Integrity Check. The valid
service token itself was verified independently with an explicit
`tve-m6-c2-live-smoke/1` User-Agent and returned 200; the smoke now uses that
explicit agent and its regression test passes. No Access bypass or public
origin fallback was introduced.

Final local checks after the fix: Ruff exit 0; full pytest `6320 passed, 2
skipped`; frozen pnpm install exit 0; Dashboard lint/typecheck/build exit 0;
Vitest `15 passed`; OpenAPI drift, generated API type drift, client-bundle
secret scan, local cross-stack smoke, deployment-helper tests and live smoke
all exit 0. A separate secret scan found zero matches in tracked files,
generated client/build output and private logs. `.dev.vars` is absent.

Before owner acceptance, M6-C2 was `IMPLEMENTED /
READY_FOR_OWNER_INTERACTIVE_ACCESS_ACCEPTANCE`. The former sole remaining
item was the owner incognito-browser sequence: Access wall before Dashboard,
approved identity can read the overview and one detail, and a fresh
unauthenticated session cannot see Dashboard. That result is recorded below.
The automated smoke remains the authoritative transport evidence. A6 remains
pending; no Phase 6, M5-C/R2, M4-D/M4-E or M2-D work was started.

The closure commit is `4365a950c3a5afa28656545e7fbca1ef6253f1f9`.
GitHub Actions run `35502629180` completed with `success`; its single `test`
job and all 20 required execution steps passed, including the final generated
contract, Dashboard and real local cross-stack checks.

## 12. Owner acceptance and closure — 2026-09-20

The owner confirmed the final interactive browser acceptance at
`https://tve-private-dashboard.haoqi90.top`: Access appeared before Dashboard
content, the approved identity login succeeded, `HK00288` was verified, and a
new unauthenticated session could not see the Dashboard. This closes M6-C2 as
`CLOSED / OWNER_ACCEPTED`.

The owner reported the following future Dashboard improvements:

- add Chinese-first user-facing localization instead of an English-only UI;
- replace developer terminology and raw technical errors with plain-language
  explanations, while keeping technical diagnostics out of normal user paths;
- replace placeholder-like content with explicit empty/partial-state designs;
- explain `NOT_AVAILABLE`, `BLOCKED`, `NOT_EVALUATED` and `SPECIAL_REVIEW` in
  user-facing language, including why a value is unavailable and what source
  or action is missing;
- improve overview/detail readability for company name/code, dates, units,
  currency, provenance and source status.

These are a future Dashboard UX/localization package. They do not change the
read-only API, deterministic investment semantics, source selection, Access
boundary or M6-C2 deployment, and no Phase 6 work was started.

The final closure-documentation commit is
`4dd935d01c4d899e0aab68ece7d720b54781baa8`; GitHub Actions run
`35503260175` completed with `success`, including all 20 required execution
steps.


## 13. Post-M6-C2 audit and next-package selection — 2026-09-20

The repository was re-audited at
`8be5d511d432c08424077dbc142a4e86468b215f`. GitHub Actions run
`35503386115` for that exact baseline completed with `success`. The preceding
M6-C2 closure commit `4dd935d01c4d899e0aab68ece7d720b54781baa8`
was independently green in run `35503260175`; its single `test` job passed
all 20 required execution steps.

M6-C2 is therefore accepted as closed. The owner-visible issues recorded during
the real deployment are presentation defects, not security/deployment blockers.

The selected next package is M6-C3:
`docs/goals/phase-5r-a-m6-c3-dashboard-ux-localization.md`.

M6-C3 is deliberately smaller than Phase 6. It makes the existing read-only
Dashboard Chinese-first, centralizes semantic state explanations, improves
empty/partial/error presentation, and moves technical identities/errors behind
progressive disclosure. It does not change the surface schema, M6-B API,
Cloudflare Access/Tunnel policy, source selection, A6/PIT contracts or
investment semantics.

Implementation has not started. Phase 6 watchlist/event-driven re-analysis
remains the next major functional phase after this presentation package closes.
A6 remains a separate strict production-claim audit and is not a prerequisite
for the personal Dashboard path.

Verification policy for M6-C3 explicitly prefers the permanent owner test
environment under `D:\code\research`, requires Codex to execute every
automatable Python/frontend/cross-stack/CI check itself, and permits manual
verification only for a bounded browser/layout check when the available
environment genuinely cannot automate it. Exact fallback steps are written in
the M6-C3 goal.
