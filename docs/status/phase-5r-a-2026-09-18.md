# Phase 5R-A 当前状态 — 2026-09-18

状态：**ACTIVE / PARTIAL；M4 CLOSED / PARTIAL；M5-A/B COMPLETE；M6-A NEXT；PRIVATE RESEARCH 可用；A6 严格审计仍 BLOCKED**

## 1. 本次 owner-authorized BaoStock 运行结果

Owner 已确认 BaoStock 仅用于本人本地、非公开研究，不上传、不再分发；该声明已写入
私有 acquisition plan 的 limitation/notes，不包含任何 API key。这个私有使用范围不以
“免费开放许可”作为前置条件；来源条款和访问声明只作为 provenance/audit metadata 保存，
不作为本地研究、私有缓存或离线 replay 的 blocker。可选依赖已安装在本机 `.venv`，
版本为 `baostock 0.9.3`，SDK runtime version 为 `00.9.30`。

使用官方来源 URI 的私有计划：

```text
.tve-private/plans/baostock-a-m3-official.json
target: SH600000
window: 2024-01-01..2024-01-05
calendar: SSE
source_uri: https://www.baostock.com
```

`historical source probe --network=allow` 的两个 request 均为 `PASS`：

- `query_stock_basic`：HTTP 200，1 条 `sh.600000`，`ipoDate=1999-11-10`，当前
  `status=1`、`outDate=""`；response SHA-256 为
  `7ce7877626582f44da452cb354587ce3b2e6e5fda738a7f060e9ef4036a38f5a`；
- `query_trade_dates`：HTTP 200，完整返回 2024-01-01..2024-01-05 五个日历日期，
  其中 2024-01-01 为非交易日，其余四日为交易日；response SHA-256 为
  `f721d5ddd363c9526f38b58adb5962f34c0e69bc29da2e4a5e3241e6fd533497`。

该私有计划还保存了来源页面的 URI/hash 和 owner access-grant reference。它们用于
追溯，不代表项目声称数据可公开再分发，也不要求项目先证明“免费开放许可”。

`acquire` 已将两个 decoded SDK export 保存到私有 raw CAS：

```text
batch:   batch-5362e2892358f7d5a751e429ab5959cc
batch sha256: e42152e6b497b11823df6d86914912cdb372d755c8e16bb560a4bebcc9da981b
aggregate: 68d6af36f1753042445e7544a85dfed0dc3385016f72f5b28534c13e07c7d2cf
```

随后完全离线执行 `compile --verify-replay`，生成：

```text
dataset: dataset-3479b2fb514facbd09566ddee1a30958
LISTING_LIFECYCLE rows: 1
TRADING_SESSION rows: 5
```

两个 JSONL shard 的 content hash 为：

```text
LISTING_LIFECYCLE: f9793387d684b8f3cb6f3330d48842f0013030802fba0769b766799c938e2a0b
TRADING_SESSION:   1e1125277e3d47310f89cc28b1918b2c8287c6edc1f154017dc54b6d5160bd19
manifest:          510c78429a2ded912e53d37a30c2ea813ed11a04378353c6b178b972747004d8
```

这证明当前 M3 的真实获取、私有 CAS、source lineage、生命周期/calendar 编译和
离线 replay 路径可运行；不证明完整 A/H historical corpus。

## 2. 为什么完整 validation/accept 仍失败

这是预期的 claim-level fail-closed，不是 BaoStock 调用失败。最小 bounded plan 没有
声明或获取以下内容：

- 有效日期的 universe membership；
- A 股价格、corporate actions、benchmark、FX；
- 官方 filing；
- 两整年目标范围；
- H listing；
- 独立 reconciliation；
- terminal/delisting economics。

因此 `dataset validate` 明确报告 target listing 在起始日没有 source-backed membership
interval，`historical accept` 保持 BLOCKED。这里的失败来自历史数据覆盖和 A6 claim
要求，不是“免费/开放许可”问题。私有计划已经记录 owner 的非公开使用范围、来源
provenance 和 access-grant reference；这些记录不会把当前 bounded slice 夸大成完整
历史市场数据，也不会把它声明成可公开再分发数据。

换句话说：这批数据对本人的私有研究、私有缓存和离线 replay 已经可用；只有当项目要
通过严格 A6/production audit 或做更宽的 historical-corpus claim 时，下面的数据覆盖
缺口才会继续阻塞。

## 3. 当前仍然需要的工作

1. 如果只做固定 A 股研究集合，M3 已足够继续；需要另建包含 A 股价格/行为/filing 的
   bounded plan，不必先解决市场级 historical membership。
2. 如果要声称历史指数/全市场 membership，需要另一个提供有效日期成分和 code change
   的来源；BaoStock `stock_basic` 当前接口不能证明这一点。
3. 如果要做生产级回测，需要退市、收购、转板、长期停牌和终值的明确来源与 policy。
4. H-share 仍只有 `H_PRICE_SOURCE_SELECTED_FUTU` 的 bounded unadjusted daily price role；
   M2-D 未实现，H membership/lifecycle/action/terminal/complete coverage 仍不成立。

M2-E 结论保持不变，M2-D 继续 gated。下一代码目标仍为 M4 cross-source reconciliation，
但只有在选定第二个可用来源后才有实际 reconciliation 输入。


## 4. M4 planning handoff

M4 dedicated planning is now frozen in
`docs/goals/phase-5r-a-m4-cross-source-reconciliation.md`.

For the fixed private A-share research set, the planned first vertical slice is:

- keep the existing Hithink unadjusted daily A-share path as the canonical price
  source for the bounded sample;
- require provider/upstream-level independence rather than treating different
  adapter IDs as independent evidence;
- evaluate an additive BaoStock sampled unadjusted daily price-reference path
  through the same private-CAS/offline-replay boundary;
- reuse the existing deterministic scalar reconciliation contract for sampled
  close prices;
- treat corporate-action/reference events and lifecycle/status as separate
  semantics, using additive typed/versioned audit artifacts when the scalar
  price contract is insufficient;
- prefer official SSE/SZSE/BSE evidence for the applicable listing when
  cross-checking lifecycle facts.

This planning decision does not prove that any second live source has already
passed. A failed or correlated candidate remains an explicit blocker. M2-E
`H_PRICE_SOURCE_SELECTED_FUTU` remains unchanged, M2-D remains gated, and no
H-share coverage is expanded.

The active Codex handoff is
`docs/status/phase-5r-a-next-codex-goal.md`.

## 5. M4-A/B/C code audit at baseline `a0469f4`（historical handoff）

The following bullets preserve the pre-closure audit that motivated this
package; the current implementation and owner run are recorded in §§6–7.

审计结论需要比上一版状态更严格：

- **M4-A：deterministic complete。** `historical_reconciliation_sample_spec_v1`
  和 provider/upstream independence guard 已实现；相同或 unresolved upstream 会
  fail closed。
- **M4-B：deterministic complete。** 新增
  `baostock-a-share-price-reference`，显式使用 daily / unadjusted / CNY
  `query_history_k_data_plus`，完整 decoded SDK export 先进入 private-CAS-compatible
  envelope，offline decoder 再生成 `MARKET_BAR`；M3 lifecycle adapter 语义未改变。
- **M4-C：comparator core complete，但 acceptance 尚未完全闭合。**
  `reconcile_sampled_market_bars` / `reconcile_sampled_prices` 已实现
  PASS/FAIL/PARTIAL/MISSING、union missingness、basis/CNY/scope/independence guard
  和 deterministic content hash。**但当前没有一个 M4 专用的 artifact-backed
  execution/persistence path，从两个冻结的 `HistoricalArtifactStore` /
  manifest shard 读取 canonical Hithink 与 independent BaoStock rows，再按 persisted
  sample spec 生成并持久化 report。**
- 现有通用 `tve dataset reconcile` 仍读取任意 JSON value arrays，并直接调用
  `reconcile_observations`；它不强制 persisted sample spec、provider/upstream
  identity、CNY/unadjusted basis 或 frozen artifact provenance，因此不能单独作为
  M4-C closure evidence。
- `tests/test_m4_reconciliation.py` 已覆盖 fake BaoStock acquire -> CAS -> offline
  compile/replay、independence、schema drift、adjusted/H/out-of-range/duplicate、
  PASS/FAIL/PARTIAL/MISSING 和 repeated hash；仍应补一个 artifact-backed two-source
  end-to-end test，以及精确 tolerance-boundary test。

提交 `a0469f4` 的状态文档记录 `python3 -m ruff check .` PASS、
`python3 -m pytest -q` 为 `6243 passed, 2 skipped`；本次 ChatGPT 审计环境无法
重新 clone/run（容器无 GitHub DNS），GitHub commit 也没有附加 CI status，因此这些
数字只能作为仓库已有运行记录，不冒充本次重新执行结果。

上述 **M4-C artifact-backed closure** 已在后续实现中补齐。若 owner runtime
能访问私有 Hithink shard 和 BaoStock SDK，再执行最小 owner-authorized
BaoStock price probe/acquire/compile/reconcile，以获得真实 independent-source
evidence；该 live run 是 M4 real-evidence closure 所需，但不是代码实现的阻塞。

M4-D corporate-action/reference-event audit 与 M4-E official-exchange lifecycle
cross-check 均属于后续可选扩展；不得把它们描述成当前 M4-C 的阻塞。M2-D 继续明确
不实施，M2-E `H_PRICE_SOURCE_SELECTED_FUTU` 和 H-share scope 均保持不变。

## 6. M4-C deterministic closure（本次实现）

新增的 `reconcile_sampled_market_bars_from_artifacts` 只读取两个显式冻结的
`HistoricalDatasetManifest`/`HistoricalArtifactStore` 输入，并按持久化
`HistoricalReconciliationSampleSpec` 选择 listing/date 范围。它核对 source、adapter、
provider、upstream identity，强制 A-share/CNY/UNADJUSTED 语义，复用既有
`HistoricalReconciliationReport`，并可将该报告写入独立 content-addressed JSON store。
旧的通用 `tve dataset reconcile` JSON-value 接口没有改变。

本次离线验证覆盖：

```text
python3 -m pytest tests/test_m4_reconciliation.py -q -> 19 passed
python3 -m pytest tests/test_phase_5r_historical.py -q -> 25 passed
python3 -m pytest tests/test_phase_5r_acquisition.py -q -> 60 passed
python3 -m pytest tests/test_baostock_historical.py tests/test_fqgate_historical.py tests/test_futu_opend_historical.py -q -> 68 passed
python3 -m ruff check <changed files> -> PASS
```

## 7. Owner-live M4 price evidence（本次运行）

当前运行时具备 owner 私有 Hithink `SH600000` 冻结 shard 与 BaoStock SDK，因此按最小
2-session overlap 执行了 owner-authorized probe → acquire → offline compile/replay →
artifact-backed reconciliation。只记录非秘密摘要：

```text
sample:       SH600000 / 2020-01-02..2020-01-03 / 2 observations
tolerance:    absolute=0.02, relative=0.001
canonical:    hithink-a-prices / hithink-market-dumps / hithink-financial-api
independent:  baostock-a-m4-price / baostock-a-share-price-reference / baostock
probe:        PASS; response sha256=d96a96304598d493458eb1e473a0d85bad45d955c070caf09f7b317abefc50ec
batch:        batch-e5f3ec281025db7a8a98f21a83a95203; sha256=3d83e98f81fb33edadeedc35393fa0d5c81b44b3ec424bf764358404b451b7d8
canonical frozen:   dataset-b526a1d6c173782ba80cd28ea29a8daf-m4; manifest sha256=59f5775e0bed90f32255fc3137bb079f30b51a1fbe6f64e4283a9dcd73fe9d88; shard sha256=c556c2ec8fd9b1fa7daa65e3541daff5c86e0018779b4dd657fccb2b6a538bf1; rows=486
independent frozen: dataset-e08a5d4ed4ead6d8a2cc50c3155c3453; manifest sha256=d7fbe24f84f3735d77a1897aede108009d2b604cc7b2987b5afb50e5f53dc507; rows=2
sample spec:  sha256=a0c19e20aa6feea657279139ba923b9e32e07d75e2c3ebf84fddef5b19c22f65
report:       PASS; 2/2 comparisons PASS; content sha256=f0fc94fd2d9fd051bbcd5269440f4cc6d49f5ea8e01d903945c2be7db1c2300a
replay:       second identical offline execution produced the same report hash
```

这使声明精确到 **fixed A-share sampled price reconciliation PASS**；不表示完整市场覆盖、
历史 membership、terminal economics、corporate-action 或 H-share production claim。
BaoStock readiness 仍保留 `HISTORICAL_CAPABILITY_UNVERIFIED` 与
`TERMINAL_COVERAGE_UNVERIFIED` blockers，且 M4-D、M4-E 和 M2-D 均未实现。

按 M4 goal §9 已冻结的 closure 定义，`M4_FIXED_A_RECONCILIATION_PASS` 还要求 declared
sample 所需的 action/lifecycle checks；当前这两项被明确延后。因此这里不再把 price
PASS 等同于整个 M4 PASS：**M4-C / price reconciliation 为 PASS，而整体 M4 以
`M4_FIXED_A_RECONCILIATION_PARTIAL` 关闭**。这不阻塞个人研究，也不阻塞进入 M5。

## 8. M4-C 复核与下一阶段（2026-09-18）

对 `f17ce3c0` 的代码复核确认：

- `reconcile_sampled_market_bars_from_artifacts` 从两个显式
  `HistoricalDatasetManifest` / `HistoricalArtifactStore` 读取冻结 `MARKET_BAR`;
- source/adapter/provider/upstream identity、target/listing/date scope 与
  A/CNY/UNADJUSTED 均 fail closed；
- report 可按现有 v1 contract content-addressed 持久化；
- `tve dataset reconcile-artifacts` 是 additive CLI；legacy `dataset reconcile`
  未被改写；
- tests 已覆盖 artifact E2E、identity/upstream、scope/semantics、MISSING/PARTIAL、
  tolerance boundary、稳定 report hash 与 CLI；
- owner-live BaoStock/Hithink price evidence 已按 §7 记录为 2/2 PASS。

实现层面的 M4-C 缺口已经关闭。

当时唯一验证债务是提交记录中只写有 focused test groups 和 changed-files
ruff，GitHub commit 没有 CI status，尚未记录改动后的完整 pytest 结果。该债务已在
M5-A 编辑前以 python3 等价入口完成 baseline，并在 M5-A 完成后再次完成全量验证；
精确结果见下一节。

该复核时的下一 milestone 是 M5-A；M5-A 现已按下一节完成并关闭。M4-D/M4-E 保持可选，M2-D 保持 gated，Phase 5R/A6 整体仍为 ACTIVE / PARTIAL。

## 9. M5-A deployment-neutral artifact mirror closure（2026-09-18）

远程同步先将当前 main 从 f17ce3c 快进到 58d3edc。同步后的 M5-A 实现
新增 provider-neutral immutable object protocol、versioned mirror manifest、
offline filesystem backend、显式 plan/push/verify/pull workflow 和 additive CLI；
不包含 S3/R2、RawBlobStore mirror、remote credentials、M4-D/M4-E、M2-D 或
deterministic investment math。

镜像 inventory 只来自显式 HistoricalDatasetManifest 与其 shards：
manifest.json 加 sha256/...jsonl。源 store 中的 unrelated JSON/research
sidecar 不会因 root scan 被上传；恢复后重新通过现有
HistoricalDatasetManifest + HistoricalArtifactStore validator/compiler boundary。

验证记录：

~~~text
requested python -m ruff check .: unavailable (shell has no python executable)
requested python -m pytest: unavailable (shell has no python executable)
python3 -m ruff check .: PASS
python3 -m pytest (pre-M5 baseline): 6255 passed, 2 skipped in 13.52s
python3 -m pytest tests/test_historical_artifact_mirror.py -q: 10 passed in 0.15s
python3 -m pytest tests/test_m4_reconciliation.py -q: 19 passed in 0.06s
python3 -m pytest (post-M5 final full suite): 6265 passed, 2 skipped in 17.26s
~~~


## 10. M5-A 复核与 M5-B 下一阶段（2026-09-18）

对当前 main 的 M5-A 实现复核确认：mirror manifest、显式 inventory、
`ArtifactObjectStore`、filesystem reference backend、push/verify/pull、恢复前全对象
校验和本地 HistoricalArtifactStore revalidation 均已落地；测试矩阵覆盖了显式对象
范围、byte identity、幂等/冲突、missing/corrupt、path/symlink、tampered manifest、
no-network 和恢复后一致 replay。未发现需要重开 M5-A 的结构性缺口。

GitHub 当前 head 没有附加 CI status/workflow run；仓库提交记录已保存 M5-A 完整
`python3 -m pytest` 结果。此次 ChatGPT 审计环境尝试独立 clone/run 时因容器 DNS
无法解析 github.com 而未能重复执行，因此不把它伪装成新的测试证据。下一 Codex
goal 必须在修改 M5-B 前先重新跑 `ruff + full pytest` baseline。

下一 active milestone 为
`docs/goals/phase-5r-a-m5-b-s3-compatible-artifact-backend.md` 的 **M5-B**：
在不改变 M5-A manifest/protocol 的前提下加入一个真实 S3-compatible backend，
Cloudflare R2 为首要 operator target，同时保持 MinIO/S3-style 兼容、显式
`--network=allow`、credential references、immutable put-if-absent 和 exact-byte
SHA-256 verification。

M5-B 必须明确：S3/R2 ETag 不作为历史 artifact 的 SHA-256 身份；普通 CI 使用
fake/injected S3 client 且完全离线；真实 R2/MinIO smoke 仅为 owner-authorized
可选证明，不因缺少云账号/凭据把实现判为 blocked。RawBlobStore remote mirror、
M5-C、M4-D/M4-E、M2-D、M6/Phase 6 和 deterministic investment math 均不进入
当前任务包。

## 11. M5-B S3-compatible / Cloudflare R2 artifact backend closure（2026-09-18）

M5-B 已在同步后的 `79a64ae` 上完成：新增 optional/lazy boto3-compatible
`S3CompatibleArtifactObjectStore`，Cloudflare R2 为主要 operator target，同时支持
MinIO/S3-style endpoint。runtime config 只含 endpoint、bucket、region、prefix、
addressing style 和 credential references；resolved credentials 不进入 manifest、
object key、CLI flag、日志或结果 artifact。`push|verify|pull` 新增显式
`--backend s3 --remote-config ... --network=allow`，filesystem 默认语法和完全
offline/backend-neutral `plan` 保持兼容。

immutability 通过 `If-None-Match: *` conditional create 实现；409/412 race 会重新
读取 winner，仅相同 bytes 幂等成功，不同 bytes fail closed。remote stat 只采信显式
`tve-sha256` metadata；ETag 从不替代 M5-A SHA-256，最终 verify/pull 仍对下载 bytes
按 mirror manifest hash/length 校验。missing、permission/auth、transport 和 config
错误保持不同且不回显 secret。pull 最终写入并验证本地 HistoricalArtifactStore；
没有 RawBlobStore remote mirror、M5-C、M4-D/M4-E、M2-D、M6 或 deterministic math
改动。

本次精确验证：

~~~text
python -m ruff check .: unavailable (shell has no python executable)
python -m pytest: unavailable (shell has no python executable)
python3 -m ruff check .: PASS
python3 -m pytest tests/test_historical_artifact_mirror.py -q: 10 passed
python3 -m pytest tests/test_historical_artifact_s3_mirror.py -q: 11 passed
python3 -m pytest tests/test_m4_reconciliation.py -q: 19 passed
python3 -m pytest: 6276 passed, 2 skipped
~~~

普通 CI 使用 fake/injected S3 client，无真实网络和云凭据。未执行真实 R2/MinIO
smoke；当前运行环境没有 owner-authorized cloud credential，这不是实现 blocker。


## 12. M5-B 独立审计与 M6-A 下一阶段（2026-09-18）

对当前 `main` 的 `1580670` 进行代码/契约审计后，M5-B 可保持
**CLOSED / COMPLETE**，未发现需要重开该 milestone 的结构性缺口：

- `S3CompatibleArtifactObjectStore` 仍实现 M5-A 的 narrow object-store boundary；
- remote client use 前有显式 network allow gate 与 credential-reference resolution；
- `put_if_absent` 使用 `If-None-Match: *`，409/412 race 会重新读取 winner，
  仅 identical bytes 幂等通过，不同 bytes fail closed；
- `tve-sha256` metadata 与 ETag 明确分离，最终 verify/pull 仍按 mirror manifest
  的 byte length + SHA-256 校验真实下载 bytes；
- missing、permission/auth、transport/config error 仍分离，filesystem 默认行为与
  backend-neutral/offline `plan` 未改变；
- 专门 fake-S3 测试、CLI 接线与 optional/lazy `boto3` extra 均已落地。

仓库已有 M5-B 完成运行记录：`ruff PASS`、M5-A mirror 10 passed、S3 mirror
11 passed、M4 reconciliation 19 passed、full pytest `6276 passed / 2 skipped`。
GitHub HEAD 仍没有附加 CI status/workflow run。本次 ChatGPT 又尝试独立
`git clone` + 复跑，但当前容器 DNS 无法解析 `github.com`，因此**不把仓库中已有
测试数字冒充为本次重新执行结果**。这不构成 M5-B blocker；下一 Codex goal 应在
编辑前重新跑 `ruff + full pytest` baseline。

下一 active implementation package 已冻结为：

`docs/goals/phase-5r-a-m6-a-read-only-research-surface.md`

M6-A 先建立稳定、versioned、schema-backed 的只读 research surface，把已经验证/
冻结的 `CompanyAnalysis`、trace/report 以及可选 historical acceptance/readiness
状态投影成一个适合 Hermes/Skill/API/后续 Web Dashboard 消费的派生 artifact。
它只投影既有结果，不重算 CDC、Net Cash、Through Return、hard gates、valuation
或 Business Quality；partial/missing/A6-blocked 必须原样可见。

M5-C 继续为**可选** artifact-class/policy expansion，不作为 M6-A 前置。HTTP API、
Cloudflare Worker/D1、UI、remote publish、watchlist/event monitoring、M4-D/M4-E
和 M2-D 都不进入 M6-A。
