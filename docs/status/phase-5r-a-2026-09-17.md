# Phase 5R-A 当前状态 — 2026-09-17

状态：**ACTIVE / PARTIAL；M2-E `H_PRICE_SOURCE_SELECTED_FUTU`；M3 CODE COMPLETE；LIVE PROBE PENDING（截至本日）**
M2-B 证据关闭基线：`ccd3c7f85034506cfe1520df486faab3e32bdd65`  
M2-T 代码基线：`2ee554daba24d988de38d348137b7a0a31c56e69`  
M2-E 起始基线：`78f3f5801f269f4757e3f1fb7c8a29139641bbd3`

## 1. 已完成到哪里

Phase 5R-A M2-A 已完成并进入 `main`。M2-T 也已在 Turtle 侧完成：FQGate
通过显式 endpoint topology 支持 `LOCAL_DIRECT` 与 `REMOTE_BRIDGE`，同时保留
legacy adapter/decoder；在不发明 provider 语义的前提下区分本机连接失败、远端
endpoint/auth/HTTP 层、结构化但语义未知的 provider code、无行、部分覆盖和
schema 不支持。普通 CI 仍完全离线。

M2-B 已完成 owner-side live evidence gate。当前已获得的事实是：

- A-share `USHA/600000`、`USZA/000001` 的有界 FQGate 历史请求曾成功；
- owner-observed H candidate `UHKM/HK0700` 在两个不重叠历史窗口均未获得成功历史行；
- 两个 M2-B live probe 都真正到达 FQGate history endpoint，而不是 runner/local-host
  不可达；
- FQGate 因此没有获得 bounded H `MARKET_BAR` source role；
- bounded H `MARKET_BAR` 现在由 Futu OpenD 获得 `H_PRICE_SOURCE_SELECTED_FUTU`；
- Futu OpenD 已获得 bounded H `MARKET_BAR` source role；AKShare/Eastmoney 不启动；
- M2-E 已把上述选择固化为机器/审计可读记录，并关闭 M2；
- 更宽的 H historical membership/lifecycle/action/terminal/source-terms 完整性仍未证明，
  因而不把 bounded 价格选择升级为完整 H corpus 声明；
- `strict-v1`、A6、CDC、Net Cash、Through Return、hard gates、valuation 均未改变。

M2-T 实现后的完整代码验证记录为：

```text
python3 -m pytest tests/test_fqgate_historical.py -q -> 44 passed
python3 -m pytest tests/test_phase_5r_acquisition.py -q -> 60 passed
python3 -m ruff check . -> PASS
python3 -m pytest -> 6212 passed, 2 skipped
```

M2-B 本身是 live evidence / documentation closure，没有修改 deterministic engine 或
historical compiler 代码。GitHub 当时没有为 M2-B closure commit 暴露额外的 commit
status checks，因此不要把“无 status context”描述为一次新的 CI 通过记录。

## 2. 当前跨仓库事实

`blooddrunk/fqgate-remote-bridge` 当前已关闭其本地 upgrade center + runtime
OpenAPI/docs foundation。其后续 roadmap 把 secure remote human access 与 machine
read-only API 分开：Tunnel/Access 与稳定 machine-authenticated read-only market-data
contract 仍属于后续阶段。

因此 Turtle 不应继续把“FQGate provider”与“本机 loopback 拓扑”绑定成同一概念，
但也不能因为 remote-bridge 已推进本地能力就声称存在可供 Turtle 消费的远程机器 API。
未来同一个 FQGate 历史数据请求仍可能来自：

```text
LOCAL_DIRECT  -> 127.0.0.1:17281 -> FQGate
REMOTE_BRIDGE -> HTTPS/Access/Tunnel -> fqgate-remote-bridge -> loopback FQGate
```

Tunnel/Access/FQGate lifecycle 仍归 remote-bridge 所有，Turtle 只消费显式配置的
market-data endpoint。`REMOTE_BRIDGE_LIVE_UNPROVEN` 继续成立，但它不阻塞当前 H-share
source selection。

## 3. M2 执行顺序

```text
M2-A  FQGate diagnostic hardening                   COMPLETE
M2-T  endpoint/topology + auth/diagnostic boundary COMPLETE (Turtle-side)
M2-B  bounded H FQGate selection probe             COMPLETE (evidence-supported failure)
M2-C  Futu OpenD COMPLETE (`FUTU_SELECTED` bounded H MARKET_BAR role)
M2-D  AKShare/Eastmoney fallback/reconciliation    GATED
M2-E  selection record                             COMPLETE (`H_PRICE_SOURCE_SELECTED_FUTU`)
M3    A-share lifecycle/calendar automation        CODE COMPLETE; live probe pending
```

M2 的原则是“一个候选赚到 bounded H price role 后停止 provider proliferation”。M2-C
不是为了把 Futu 变成完整 historical corpus authority，而只是判断它能否成为当前最小可用的
H-share unadjusted daily `MARKET_BAR` source。M2-E 只固化选择结果，没有扩大 provider
能力或 coverage 声明。

## 4. M2-T 关闭边界

M2-T 已完成以下 Turtle-side contract：

- legacy `fqgate-local-market-history` v1 plans/receipts/batches 保持可读取和离线
  replay；新增 deployment-neutral `fqgate-market-history` adapter/decoder；
- 新请求显式写 `endpoint_kind` 与支持的 `fqgate-envelope-v1` contract；LOCAL_DIRECT
  只接受 loopback FQGate history endpoint，REMOTE_BRIDGE 只接受显式 HTTPS operation
  endpoint，不做 local/remote fallback 或 route discovery；
- 复用 `credential_ref`、resolver 与现有 header transport seam；credential 不进入
  persisted artifacts；
- remote transport/auth/HTTP 与 FQGate entitlement/provider diagnostics 分层；
- 内置 transport 默认拒绝 redirect，避免 authenticated remote request 泄漏。

remote-bridge 未来真正发布稳定 machine market-history contract 后，再补有界 A-share
dual-path smoke proof；该 smoke 只证明 transport compatibility，不自动升级 H 或完整
historical capability。

## 5. M2-B 执行结果：证据支持的 FQGate 失败

M2-B 使用 owner 实际观察到的 `UHKM/HK0700`（canonical listing `HK00700`），仅作为
两个私有计划中的 exact candidate，没有扩展成通用 H market mapping。此前 runner 无法
访问本机时的结果是 `BLOCKED_ON_OWNER_LIVE_PROBE`；owner 启动 FQGate 后，重新通过
`fqgate-market-history` adapter、`LOCAL_DIRECT`、`fqgate-envelope-v1` 和显式
`--network=allow` 执行了两个不重叠窗口：

- `2026-09-08` 至 `2026-09-11`：private report
  `.tve-private/live/fqgate-h-recent-20260908-11-owner-retry.json`，report SHA-256
  `99ee47ec0d973f63863cd7f3f63000a398ef7f702ccb8b5d204a6ce32677bdd7`，HTTP 504；
- `2025-09-08` 至 `2025-09-11`：private report
  `.tve-private/live/fqgate-h-older-20250908-11-owner-retry.json`，report SHA-256
  `3295f8c6408c843aedb366070e48690faa61f03ffabcb3a728e82232a16e9388`，HTTP 504。

两次 report 都记录 `network_used=true`、`historical_capable=false`、没有 observed H
rows，并保守记录：`FQGATE_HTTP_504_UNCLASSIFIED: HTTP 504`、provider code `4003` 及
`FQGATE_PROVIDER_ERROR_UNCLASSIFIED`。这证明当前实际 FQGate 历史请求没有为该 exact
identity 提供受支持的 H 日线，因而未能获得 M2-B bounded `MARKET_BAR` role；但没有
证明具体是路由拒绝、上游 timeout、权限、provider outage 或其他根因，`4003` 未被映射。

由于双窗口没有成功，没有运行 acquisition、raw CAS、compile 或 `--verify-replay`；这
符合“仅双窗口成功后才执行”的规则。`realtime/hk` 的正常返回属于实时接口证据，不能
替代历史 `/v1/market/history/klines` probe。

## 6. M2-C 执行结果：`FUTU_SELECTED`

M2-C 分为两个连续但不同性质的 gate：

```text
M2-C1  adapter / provenance / compiler integration + offline fake-client tests
M2-C2  owner-runtime recent + older live probe and bounded acquire/replay evidence
```

M2-C1 已完成：Futu Python SDK 通过 lazy/injected boundary 接入，普通 import 和离线
compiler 不依赖 SDK；adapter 明确发送 daily `K_DAY` 与 unadjusted `NONE`，在请求前
检查可用历史 quota，使用有界 page key 分页并关闭 OpenD context；完整 decoded SDK
结果冻结为 `futu-opend-sdk-export-v1` provider envelope 后才进入既有 raw CAS 和
offline `MARKET_BAR` compiler。FQGate 字段不会被用来推导 Futu identity。

M2-C 验证记录：

```text
python3 -m pytest tests/test_futu_opend_historical.py -q -> 18 passed
python3 -m pytest tests/test_phase_5r_acquisition.py -q -> 60 passed
python3 -m ruff check . -> PASS
python3 -m pytest -> 6230 passed, 2 skipped
```

M2-C2 的实际结果为 **`FUTU_SELECTED`**。真实 owner OpenD `10.11.7108` 在
`127.0.0.1:11111` 达到 Ready；`futu-api 10.10.7008` 连接成功；港股股票列表返回
3,790 行并实际返回 `HK.00001`；历史 K 线额度预检为 `used=0, remaining=100`。
同一运行时 identity 的两个非重叠、有界、日线未复权窗口均成功，并明确使用
`K_DAY + AuType.NONE`：

```text
2026-09-15..2026-09-17 -> PASS, HK.00001, 3 rows
2025-09-15..2025-09-17 -> PASS, HK.00001, 3 rows
```

两次 probe 成功后，才执行 recent 窗口的 bounded acquire -> private CAS/provider
envelope -> offline compile -> `--verify-replay`。结果为 3 条 canonical `MARKET_BAR`；
private provider envelope 的 representation 是 `futu-opend-sdk-export-v1`，SHA-256
为 `ee75f454b86aaf98e71b1bedff4443e743a97d7d67b1ca52d2da817e33d94be2`，batch 为
`batch-d1ef177190b7cd1f25870315fb15af7b`，dataset 为
`dataset-b27e7ffc96bf7de78055d5bd3774b897`，manifest SHA-256 为
`e1aac2fd22350d0898bb718a68febb558a4ba35638cf504a1e038f15787a341b`，`MARKET_BAR`
shard SHA-256 为 `6b4708a74530f5de9bba0fc2dcfe98337976c92a6565cb9feaa5b537e2bc3a62`，并记录
剩余历史额度 99。编译输出的 readiness 仍是 `NOT_READY`，因为本目标没有提供
membership、lifecycle、terminal、corporate actions、benchmark/FX、filing 或完整 source
terms；这是预期的 fail-closed 范围，不是 Futu bounded price role 失败。

因此 `H_PRICE_SOURCE_SELECTED_FUTU` 对 bounded H unadjusted daily `MARKET_BAR` 成立；
更宽的 `H_SOURCE_UNQUALIFIED`/coverage blockers 仍只适用于上述未覆盖的历史声明。
M2-D 保持 gated 且未实现。

## 7. M2-E 执行结果：选择记录关闭 M2

M2-E 没有新增 provider、schema 或 investment-engine 状态。机器/审计可读的选择记录位于：

```text
docs/status/phase-5r-a-m2-e-h-share-source-selection.json
```

对应关闭说明位于：

```text
docs/goals/phase-5r-a-m2-e-h-share-source-selection-record.md
```

该记录只保留非秘密、已由 M2-C 获得的事实，包括：

- 唯一 bounded selection state：`H_PRICE_SOURCE_SELECTED_FUTU`；
- role：`primary bounded personal-research H daily unadjusted MARKET_BAR source`；
- OpenD/SDK 版本与 `HK.00001`；
- 两个已验证窗口及 `K_DAY + AuType.NONE`；
- quota/permission 的 bounded owner-runtime 结果；
- `futu-opend-sdk-export-v1` representation；
- provider envelope、batch、dataset、manifest、shard 与 replay identity；
- `.tve-private/` 仅作为私有证据位置，原始证据没有进入 Git；
- 完整 H corpus、membership、lifecycle、delisting、terminal economics、corporate
  actions、benchmark/FX、filings、complete coverage 和 source terms/redistribution
  仍未被证明。

当前没有第二个真实、独立且成功 replayable 的 H 来源，因此 M2-E 没有执行
cross-source reconciliation，也没有为制造 comparison 而实现 M2-D。未来若有独立来源，
仍必须使用现有 historical reconciliation contract，而不能静默改写 price rows。

M2-E 仅为文档/状态关闭包，不改变 `strict-v1`、A6、PIT、historical coverage semantics、
投资计算或既有 provider compatibility。

## 7. M3 执行结果：A-share lifecycle/calendar automation

M3 已在 Turtle 侧完成最小代码边界：

- 新增 lazy/injected BaoStock SDK adapter，使用 `query_stock_basic` 获取 A 股
  `ipoDate`/`outDate`/`type`/`status` 等基本字段，并映射到现有
  `historical-listing-lifecycle-v1` source-aware lifecycle contract；
- 使用 `query_trade_dates` 冻结显式 `historical-trading-session-v1` rows，保留交易日与
  非交易日标记、calendar ID、timezone 和 source lineage；
- SDK 解码结果先冻结为 `baostock-sdk-export-v1` raw envelope，再由 offline compiler
  生成 content-addressed JSONL shards；ordinary import/CI 不需要安装或调用 BaoStock；
- calendar rows 可自动提供价格 coverage 的 expected sessions。缺少行情仍是 missing/
  `PARTIAL`，不会被解释成停牌或零值；旧 manifests 没有声明 calendar shard 时保持原有
  A6 行为。

M3 的代码/contract closure 不等于 live source acceptance。截至本状态日期，BaoStock
owner-side probe 尚未运行；后续实际 live/replay 结果记录在
`docs/status/phase-5r-a-2026-09-18.md`。默认 economic-company ID 仅是明确标记的 A
listing-local surrogate，不是 A/H company mapping。

M3 不修改 `strict-v1`、deterministic investment math、M2-D 或任何 H-share coverage claim。

M3 本轮离线验证：

```text
python3 -m ruff check src/turtle_value_engine/historical/baostock.py tests/test_baostock_historical.py -> PASS
python3 -m pytest tests/test_baostock_historical.py -q -> 6 passed
python3 -m ruff check . -> PASS
python3 -m pytest -> 6236 passed, 2 skipped
```

本环境没有 `python` 命令；按 handoff 中的原始命令执行会返回 127，以上使用
可用的 `/usr/bin/python3` 完成等价验证。这不是项目代码 blocker。

## 8. 下一执行入口

本轮 M3 目标记录：

`docs/status/phase-5r-a-next-codex-goal.md`

M2 已关闭；截至 2026-09-17 的下一目标为 **M3 — A-share lifecycle/calendar automation**。
其 live/replay closure 后续转入 `docs/status/phase-5r-a-2026-09-18.md`，再进入
**M4 cross-source reconciliation**。继续保持：普通 CI 离线、private live artifacts 不入 Git、
不得把 bounded price success 扩大成完整 H corpus/coverage 声明，也不得改变
`strict-v1` / A6 / PIT / historical coverage semantics。
