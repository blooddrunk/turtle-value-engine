# Phase 6-A selection audit — 2026-09-21

Status: **PHASE 6-A IMPLEMENTED / CLOSED at the offline integration boundary**

## 1. Baseline audited

Current main at audit start:

- commit: d58f0239717a5e2b5a38b899f12ac2e17cbb92aa
- commit title: record final M6-C3 closure CI
- GitHub Actions run: 35550695695
- run conclusion: success

The implementation commit ff38fdd57b5dab01efbc8bb6803ea2599ee03454 changed the expected Dashboard presentation files and added the centralized presentation layer plus deterministic tests. The closing main commit is two commits ahead of the pre-M6-C3 baseline and contains no unrelated Phase 6 implementation.

## 2. M6-C3 audit result

No functional blocker was found in the audited M6-C3 closure.

Verified evidence includes:

- zh-CN is the default presentation locale and the optional English switch is client-only;
- unknown semantic-state strings use an explicit unknown/caution presentation and are not treated as pass;
- null/undefined/non-finite scalar values do not become zero;
- known API failures have friendly presentation copy while raw diagnostics remain in technical details;
- exact analysis/surface/hash identities remain reachable through technical details;
- app tests explicitly assert that save/approve/delete/trade/order/run and Chinese mutation equivalents are absent;
- full local regression, browser desktop/mobile checks, unchanged-boundary Cloudflare redeploy and live smoke are recorded;
- the exact current main commit has a green push CI run.

## 3. Documentation defect corrected

The M6-C2/M6-C3 closure record used both HK0288 and HK00288 for the same private surface. The owner/browser acceptance and M6-C3 test instructions use HK00288, and the five-digit H-share listing form is the consistent repository identity here. This audit normalizes the stray HK0288 references to HK00288.

This was a documentation identity typo, not an engine/API/surface-schema defect.

## 4. Next package selection

The selected next package is:

**Phase 6-A — Watchlist State and Deterministic Event Planning Foundation**

Goal:

docs/goals/phase-6-a-watchlist-event-foundation.md

The first slice is intentionally offline. It builds deterministic watchlist/event/state/cursor/re-analysis-plan contracts plus a local atomic workspace and CLI replay boundary before any live polling or scheduler is allowed.

A6 remains a separate strict production-claim audit and is not a prerequisite for a bounded personal watchlist. M4-D/M4-E, M2-D and M5-C/R2 remain deferred.

## 5. Verification policy

Phase 6-A is designed to require **zero planned manual functional acceptance**.

Codex must:

- prefer the permanent owner test root D:\code\research when that checkout is actually available;
- synchronize main with fast-forward only;
- require green CI for the exact baseline before editing;
- run deterministic focused monitoring tests plus the complete current repository CI gates it can execute locally;
- verify the exact pushed closing commit in GitHub Actions;
- never use "manual verification recommended" as a substitute for duplicate/cursor/atomicity/PIT tests.

Live provider credentials, schedulers, notifications and cloud state are explicitly Phase 6-B or later and must not be used as excuses to leave Phase 6-A evidence incomplete.

## 6. Implementation closure record (2026-09-21)

### 6.1 Environment and baseline

- Agent checkout: `/home/jelinenaro/research/turtle-value-engine`, synchronized
  fast-forward-only to baseline `473c6461f178fe08af7f5ff223ef55bf5aa572c7`
  (`git status --short --branch` clean; remote
  `https://github.com/blooddrunk/turtle-value-engine.git`).
- Baseline GitHub Actions run `35552957238` for exactly `473c646` concluded
  `success` before any edit.
- Permanent owner test root `/mnt/d/code/research` (`D:\code\research`) was
  discovered and contains no `turtle-value-engine` checkout (verified with
  `find /mnt/d/code/research -maxdepth 3 -type d -name turtle-value-engine`);
  the synchronized checkout above was therefore used. This is an
  infrastructure-availability note only and waived no automated test.

### 6.2 Implemented boundary

- `src/turtle_value_engine/monitoring/`: `canonical.py` (hermetic canonical
  JSON/UTC helpers), `models.py` (`WatchlistSpecV1`, `WatchlistEntryV1`,
  `MonitoringEventV1`, `MonitoringEventBatchV1`, `WatchlistEntryStateV1`,
  `WatchlistStateV1`, `EventCursorV1`, `ProcessedEventRecordV1`,
  `EventImpactDecisionV1`, `Deferred/Skipped/ExcludedEventRecordV1`,
  `ReanalysisRequestV1`, `ReanalysisPlanV1`, `MonitoringRunV1`,
  `MonitoringStatePointerV1`, `MonitoringStatusV1`), `policy.py`
  (`event-impact-v1` exact frozen mapping + severity precedence),
  `service.py` (pure deterministic planning), `workspace.py`
  (atomic/idempotent `MonitoringWorkspace` with failure injection).
- CLI: offline `tve watch validate|replay|status` in `cli.py`; non-zero exit
  code 2 with an exact actionable blocker for malformed/conflicting input;
  `status` on an unknown watchlist prints explicit `NOT_AVAILABLE`.
- Config: additive non-secret `MonitoringSettings` (`[monitoring]`
  `watchlist_path`/`workspace_root`/`event_impact_policy` with closed
  `Literal["event-impact-v1"]`) in `config/project.py`, exported through
  `config/__init__.py`, mirrored in `config/project.example.toml` and the
  in-package template, surfaced in the secret-safe `safe_summary()`.
- Schemas: ten checked-in JSON schemas under `schemas/` with deterministic
  drift tests (`model_json_schema()` equality + Draft 2020-12 validation of
  generated artifacts and fixtures).
- Fixtures: `fixtures/monitoring/` (valid watchlist, duplicate-listing
  rejection fixture, mixed PIT/duplicate/exclusion event batch).
- No database, scheduler, notification, provider transport, model call,
  `AnalystClient`, `ResearchOrchestrator`, `run_analyze*` invocation,
  Cloudflare API or brokerage path exists in the monitoring package; import
  lines are restricted to the standard library, `pydantic` and relative
  monitoring modules (proven by static import-line scan, subprocess delta
  import test and a socket-blocked in-process CLI replay test).
- `rules/strict-v1.yaml` and `schemas/normalized-input.schema.json` are
  byte-identical to the baseline (frozen sha256 guards in
  `tests/test_monitoring_cli.py`).

### 6.3 Automated verification (local, before push)

- `python3 -m ruff check .` — all checks passed.
- `python3 -m pytest` — 6405 passed, 2 skipped (baseline was 6320 passed,
  2 skipped; +85 monitoring tests, no baseline test changed).
- Focused monitoring suites: `tests/test_monitoring_models.py` (23),
  `tests/test_monitoring_service.py` (19), `tests/test_monitoring_workspace.py`
  (20), `tests/test_monitoring_cli.py` (23) — 85 passed total, covering
  duplicate-listing rejection, stable canonical IDs/hashes, exact
  `event-impact-v1` mapping and precedence, out-of-order input equivalence,
  identical-duplicate idempotence, same-event-id content conflicts (in-batch
  and against committed state), future-event deferral without cursor
  movement, stale/already-processed skip rules, explicit
  out-of-watchlist/disabled handling, cursor advancement only through
  committed next state, injected write failure at every commit stage with
  previous state intact and retry success, corruption (truncation, field
  tampering, non-canonical bytes, tampered/missing pointer) failing closed,
  byte-equivalent replay, unknown event type rejection, missing provenance
  rejection, no-network/no-runtime proofs, ProjectConfig monitoring
  round-trip + secret-free safe summary, non-zero CLI exits with exact
  blockers, frozen strict-v1 bytes, and schema drift checks.
- Dashboard gates (interpreter/toolchain present): `pnpm --dir
  apps/dashboard install --frozen-lockfile`, `python3
  scripts/export_surface_openapi.py --check`, `pnpm --dir apps/dashboard
  api:check`, `types:check`, `lint`, `typecheck`, `test --run` (44 passed),
  `build`, `security:client-bundle` (with the CI sentinel values), `exec
  wrangler deploy --dry-run --config wrangler.jsonc --strict`, and
  `python3 scripts/dashboard_cross_stack_smoke.py` — all green; no Dashboard
  file changed.

### 6.4 Manual verification boundary

No manual functional acceptance was required or deferred. All Phase 6-A
acceptance cases in the goal document are covered by the automated suites
above; no unresolved blocker exists.
